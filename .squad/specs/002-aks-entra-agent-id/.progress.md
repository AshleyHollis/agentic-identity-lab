# Spec 002 Progress

**Spec:** 002-aks-entra-agent-id  
**Milestone:** M5 — AKS + Entra Agent ID auth exploration  
**Phase:** tasks-ready (amendment 001 pending review)  
**Last updated:** 2026-05-15

---

## Status

| Artifact | Status |
|----------|--------|
| `README.md` | ✅ Amended (001) — terminology section, references |
| `goals.md` | ✅ Amended (001) — success criterion 10 (E2E tracing) |
| `research.md` | ✅ Amended (001) — section 8 (terminology, AKS Agent Gateway tracing) |
| `requirements.md` | ✅ Amended (001) — FR-11, FR-12, NFR-07 |
| `design.md` | ✅ Amended (001) — terminology definitions, E2E tracing design |
| `tasks.md` | ✅ Amended (001) — T17–T20, updated gate criteria |
| `state.json` | ✅ Amended (001) — T17–T20 blocked, amendment record |
| `.progress.md` | ✅ Amended (001) — this file |
| `identity-notes.md` | ✅ Preserved (research integrated into research.md/design.md) |
| `infra-notes.md` | ✅ Preserved (research integrated into research.md/design.md) |

**Spec gate:** ✅ Satisfied — all spec artifacts are complete. All pre-implementation gates satisfied (T01 ✅, T02 ✅, T03 ✅ — 2026-05-15).

**Milestone status:** 🔄 In Progress — spec promotion complete; amendment 001 applied and approved; T03 security gate cleared; implementation stream now unblocked.

---

## Task Progress

| Task ID | Title | Owner | Status |
|---------|-------|-------|--------|
| T01 | Spec promotion review | Morpheus | ✅ Complete (2026-05-14) |
| T02 | Record ADR-M5-01 + ADR-M5-02 decisions | Morpheus | ✅ Complete (2026-05-14) |
| T03 | Security review + ADR-M5-03 | Trinity | ✅ Complete (2026-05-15)
| T04 | AKS TF module skeletons | Tank | ✅ Complete (2026-05-10) |
| T05 | Illustrative AKS manifest docs | Tank | ✅ Complete (2026-05-27) |
| T06 | Safe-claims allowlist extension | Trinity | ✅ Complete (2026-05-15) |
| T07 | Agent ID fixture files | Trinity | 🔒 Blocked on T06 ✅ — fixtures not yet created |
| T08 | Negative fixture tests | Trinity | 🔒 Blocked on T07 |
| T09 | Strict JWKS validation tests | Trinity | ✅ Complete (2026-05-15) |
| T10 | `AgentSidecarClient` interface stub | Neo | ✅ Complete (2026-05-15) |
| T11 | `MockAgentSidecarClient` implementation | Neo | 🔒 Blocked on T07 (T10 ✅) |
| T12 | Blueprint audience validation in Agentic Layer | Neo | 🔒 Blocked on T11 |
| T13 | Integration tests for Agent OBO boundary | Neo | 🔒 Blocked on T12+T08 |
| T14 | Post-impl architecture review | Morpheus | 🔒 Blocked on T04+T05+T12+T13+T18+T19 |
| T15 | Post-impl security sign-off | Trinity | 🔒 Blocked on T06–T09+T13+T14+T20 |
| T16 | Final validation + AKS TF CI | Tank | 🔒 Blocked on T14+T15 |
| T17 | Tracing design review *(Amendment 001)* | Morpheus | ✅ Complete (2026-05-27) |
| T18 | OTEL instrumentation — mock flow *(Amendment 001)* | Neo | ⏳ Unblocked (T17 ✅) |
| T19 | Docker Compose tracing overlay *(Amendment 001)* | Tank | ⏳ Unblocked (T17 ✅) |
| T20 | Tracing security review *(Amendment 001)* | Trinity | 🔒 Blocked on T18+T19 |

---

## M5 Gate Checklist

- [x] ADR-M5-01 decision recorded (Morpheus) — 2026-05-14
- [x] ADR-M5-02 decision recorded (Morpheus + Trinity) — 2026-05-14
- [x] ADR-M5-03 decision recorded (Trinity) — 2026-05-15
- [x] `xms_act_fct` in safe-claims allowlist; dict handling confirmed (Trinity — T06) — 2026-05-15
- [ ] Seven Agent ID fixtures committed (Trinity — T07)
- [ ] All negative fixture tests pass offline (Trinity — T08)
- [x] `AgentSidecarClient` ABC and `SidecarConfig` implemented; `MockAgentSidecarClient` pending T11 (Neo — T10 ✅ T11 🔒)
- [ ] Blueprint audience validated before any OBO exchange (Neo — T12)
- [ ] Agent OBO has zero shared state with MCP user OBO (Neo — T13)
- [x] Strict JWKS tests pass: `alg:none`, `HS*`, missing `kid`, fixture-header suppression (Trinity — T09) — 2026-05-15
- [x] AKS Terraform skeletons pass `fmt -check` and `validate` (Tank — T04+T16)
- [x] Illustrative AKS manifest docs exist with illustrative labelling and "Agentic Layer" terminology (Tank — T05) — 2026-05-27
- [ ] `python -m pytest` passes (65+ baseline) (Tank — T16)
- [ ] No real GUIDs, tenant IDs, tokens, secrets, or kubeconfigs committed
- [ ] Morpheus post-impl architecture sign-off (T14)
- [ ] Trinity post-impl security sign-off (T15)
- [ ] E2E tracing design reviewed (Morpheus — T17) *(Amendment 001)* ✅ 2026-05-27
- [ ] OTEL instrumentation specified for mock flow (Neo — T18) *(Amendment 001)*
- [ ] `docker-compose.tracing.yml` overlay specified (Tank — T19) *(Amendment 001)*
- [ ] Tracing security review complete (Trinity — T20) *(Amendment 001)*
- [x] Amendment 001 approved by Morpheus + Trinity

---

## Log

- 2026-05-27 — **T05 COMPLETE — Tank (Illustrative AKS manifest docs)**

  Created `docs/deployment/k8s/` directory with five files per FR-08 and T05 acceptance criteria.

  **New files:**
  - `docs/deployment/k8s/README.md` — context/topology documentation; explains Agentic Layer vs
    AKS Agent Gateway placement, application order, and placeholder value table.
  - `docs/deployment/k8s/namespace.yaml` — `Namespace: agent-workloads` with Workload Identity label.
  - `docs/deployment/k8s/service-account.yaml` — `ServiceAccount: agentic-layer` with
    `azure.workload.identity/client-id` and `azure.workload.identity/tenant-id` annotations;
    all-zero placeholder GUIDs only.
  - `docs/deployment/k8s/agent-gateway-deployment.yaml` — `Deployment: agent-gateway` with two
    containers: `agentic-layer` (port 8000) and `entra-agent-id-sidecar` (port 9090, localhost
    binding only). Pod annotation `azure.workload.identity/use: "true"`. Placeholder images.
    Comment explaining AKS Agent Gateway placement as a separate pod.
  - `docs/deployment/k8s/network-policy.yaml` — `NetworkPolicy: agent-gateway-sidecar-isolation`
    selecting `app: agent-gateway` pods; allows ingress only to port 8000; sidecar port 9090
    intentionally absent from all ingress rules, blocking cross-pod access per design.md §5.

  **Terminology:** All files use "Agentic Layer" for the lab app service and "AKS Agent Gateway"
  for the agentgateway.dev proxy, per ADR 0006. `apps/agent-gateway/` and `agent-gateway` Docker
  Compose service name appear as explicit legacy path/service-name references only.

  **Public-safety:** All GUIDs are all-zero placeholders. No real tenant IDs, subscription IDs,
  client secrets, tokens, or kubeconfigs. All image references use `your-registry.azurecr.io`.

  **Validation:** `python -c "import yaml; ..."` YAML syntax check passed for all four YAML files.

  **Illustrative labelling:** Every YAML file begins with:
  `# ILLUSTRATIVE REFERENCE ONLY — not applied by CI or production automation.`

  **M5 Gate Checklist item satisfied:** "Illustrative AKS manifest docs exist with illustrative
  labelling and 'Agentic Layer' terminology (Tank — T05)".

  **Gate impact:** T14 (Morpheus: post-impl architecture review) dependency on T05 is now
  satisfied. T14 remains blocked on T12, T13, T18, and T19.

- 2026-05-27 — **T17 COMPLETE — Morpheus (tracing design review)**

  Reviewed `design.md §End-to-End Tracing Design` against FR-12, NFR-07, ADR-002, and the
  trinity-tank tracing input (`amendments/inbox/trinity-tank-tracing-input.md`).

  **Review findings — five dimensions assessed:**

  1. **Span model:** Structurally sound for both mock and AKS flows. Two corrections applied:
     - Mock flow OBO span name changed from `agent_obo.exchange` (underscore/dot mixing) to
       `agentic-layer.obo.exchange` (consistent dot-separated convention).
     - AKS flow span tree expanded to show correct sidecar sub-spans (`sidecar.validate`,
       `sidecar.downstream-api`) and a clarifying note that the BFF originating traceparent is
       the remote parent for the AKS Agent Gateway's root span.

  2. **W3C traceparent propagation:** Correctly specified. Both `traceparent` and `tracestate` headers
     must be propagated at every hop. Coexistence with the existing `x-correlation-id` header is
     sound — both headers travel on outbound calls. The AKS Agent Gateway handles W3C TraceContext
     natively. No change needed.

  3. **docker-compose.tracing.yml overlay scope:** Safe — no secrets, correct ports (4317 gRPC,
     4318 HTTP, 16686 UI). One gap addressed: added a T19 implementer note about Docker network
     configuration. The base Compose stack uses named network `agentic-identity-lab`; the overlay
     must join it so services can resolve `otel-collector:4317` by DNS. T19 to handle in
     implementation. Image tag pinning (currently `latest`) is a T19 implementation concern.

  4. **Span attribute names — CORRECTION APPLIED:** design.md used short names
     (`auth.mode`, `auth.audience`, `auth.outcome`, `obo.hop`) inconsistent with the
     `identity_lab.fixture_name` precedent from T03 §9. Trinity-Tank input (TR-01.2) independently
     recommended the `identity_lab.*` namespace as collision-safe best practice. Required Span
     Attributes table updated: `identity_lab.auth_mode`, `identity_lab.aud`,
     `identity_lab.authorized`, `identity_lab.obo_hop`, `identity_lab.fixture_name`. Standard OTEL
     semconv attributes (`http.*`, `service.name`) unchanged. `service.name` for the lab Agentic
     Layer set to `agent-gateway` (Compose service name); `aks-agent-gateway` for agentgateway.dev.

  5. **Static vs dynamic tracing config split:** Appropriate. **PII ERROR CORRECTED:** The dynamic
     tracing table previously listed `jwt.sub` as a CEL example — explicit PII prohibition per T03
     §9 and ADR-002. Corrected to `request.path`/`request.method` with a prohibition note.

  **Trinity-Tank inputs assessed:** TR-01.3 sidecar sub-spans incorporated into AKS span tree.
  T-OBS-01 through T-OBS-04 recommendations already covered by T18+T19. T-OBS-05 through T-OBS-07
  and ADR-M5-04 recommendation deferred to M6; ADR-002 already covers the observability strategy.

  **Verdict: AMENDED → ACCEPTED.** Four corrections applied to `design.md §End-to-End Tracing
  Design` only. No runtime code changed.

  **Gate status after T17:** T18 (Neo: OTEL instrumentation) and T19 (Tank: Compose tracing
  overlay) are now **unblocked**.

- 2026-05-10 — **T04 COMPLETE — Tank (AKS Terraform module skeletons)**

  Created four Terraform skeleton directories per `design.md §AKS Terraform Skeleton Layout` and FR-07:

  **New files:**
  - `infra/terraform/modules/aks/` — `main.tf` (stub comments), `variables.tf` (cluster_name, location,
    resource_group_name, node_count, oidc_issuer_enabled, tags), `outputs.tf` (cluster_id, oidc_issuer_url,
    kube_config — all null placeholders).
  - `infra/terraform/modules/workload-identity/` — `main.tf` (stub comments), `variables.tf`
    (blueprint_client_id, oidc_issuer, subject, audience), `outputs.tf` (federated_credential_id — null).
  - `infra/terraform/modules/k8s-bootstrap/` — `main.tf` (stub comments), `variables.tf` (namespace,
    service_account_name, blueprint_client_id), `outputs.tf` (namespace, service_account_name from vars).
  - `infra/terraform/environments/aks/` — `providers.tf` (azurerm ~> 3.100, azuread ~> 2.50),
    `main.tf` (calls aks, workload_identity, k8s_bootstrap modules), `variables.tf` (all placeholders),
    `outputs.tf`, `terraform.tfvars.example` (all-zero GUIDs and placeholder strings only).

  **Public-safety:** No real tenant IDs, subscription IDs, client secrets, or kubeconfigs. All GUIDs use
  the all-zero placeholder pattern. `tfvars.example` uses zeroed GUIDs and placeholder strings only.

  **Validation results (all pass):**
  - `terraform -chdir=infra\terraform fmt -check -recursive` → exit 0
  - `terraform -chdir=infra\terraform\environments\aks init -backend=false` → providers downloaded, modules resolved
  - `terraform -chdir=infra\terraform\environments\aks validate` → "Success! The configuration is valid."

  **Gate impact:** T05 (Illustrative AKS manifest docs) is now unblocked.

- 2026-05-15 — **T10 COMPLETE — Neo (AgentSidecarClient interface stub)**

  Implemented `apps/shared/python/identity_lab_auth/agent_obo.py`:
  - `SidecarConfig` frozen dataclass with localhost enforcement (`http://localhost` /
    `http://127.0.0.1` only; `ValueError` on any other URL — FR-06).
  - `AgentSidecarClient` ABC with three abstract methods: `validate()`,
    `authorization_header()`, `downstream_api()` — matching the sidecar
    contract in `design.md §Agent ID Sidecar Contract` (FR-01).
  - ABC docstrings document `sanitize_claims()` as a binding implementation
    contract (Security Design Notes §10, T03 requirement).
  - Docstrings explicitly call out NFR-06 path separation from MCP user OBO
    and Azure OpenAI/Foundry managed identity paths.
  - `BLUEPRINT_AUDIENCE_PLACEHOLDER` constant exported for test/fixture use.
  - `__init__.py` updated to export `AgentSidecarClient`, `SidecarConfig`,
    `BLUEPRINT_AUDIENCE_PLACEHOLDER`.
  - 16 offline unit tests added at `tests/security/test_agent_sidecar_interface.py`
    covering: localhost enforcement (8 bad URLs, 6 good URLs), frozen config,
    abstract class contract, subclass instantiation, config property, package
    exports, and MCP OBO path separation.
  - `python -m pytest` — **91 passed** (baseline 65+). No regressions.
  - T11 is now unblocked on the Neo side; it also requires T07 (Trinity fixtures).

- 2026-05-15 — **T06 COMPLETE — Trinity (safe-claims allowlist extension)**

  1. Added `"xms_act_fct"` to `config/claims/safe-claims-allowlist.json`.
  2. Added `"xms_act_fct"` to `DEFAULT_SAFE_CLAIM_KEYS` in `claims.py`.
  3. Extended `_sanitize_value()` to handle `dict` inputs: recursively sanitizes scalar string-valued keys, bounds serialised output to 512 chars. No PII leakage path — nested dict values pass through `_sanitize_value`; only scalars survive.
  4. New test file `tests/security/test_t06_actor_claims.py` — 9 tests covering: allowlist membership, PII absence, dict handling (returns dict, empty dict, non-string key drop, truncation, nested dict, 512-char bound), and sanitize_claims integration (xms_act_fct included, oid/sub dropped, no-raise gate).

  **Security Design Notes §3 gate satisfied.** `python -m pytest` — 140 passed.

- 2026-05-15 — **T09 COMPLETE — Trinity (strict JWKS validation tests)**

  1. Created `apps/shared/python/identity_lab_auth/jwks.py`:
     - `REJECTED_ALGORITHMS` (frozenset: none/hs256/hs384/hs512, lowercase stored).
     - `ALLOWED_ALGORITHMS` (frozenset: RS256/RS384/RS512/ES256/ES384).
     - `_validate_algorithm()` — lowercases before comparison (mixed-case bypass vector closed). Returns canonical casing for PyJWT.
     - `JwksCache` — TTL 300 s, one kid-miss retry, injectable `_http_client`, `fetch_timeout_seconds ≤ 5.0`. JWKS URL from `self.jwks_url` only — jku/x5u never followed (Security Design Notes §7).
     - `validate_strict()` — header decode → alg check → kid check → cache lookup → jwt.decode. No token-header URL influence.
  2. Exported new symbols from `identity_lab_auth/__init__.py`.
  3. New test file `tests/security/test_jwks_strict.py` — 30 tests covering: alg:none (all mixed-case), HS256/384/512, empty alg, canonical form, JwksCache kid-miss/retry/stale/URL-immutability/timeout-bound, validate_strict rejection (alg variants + kid cases), fixture-header suppression, jku/x5u SSRF suppression (4 tests), RS256+kid structure accepted.

  **All M5 Gate Checklist items for T06 and T09 satisfied.** `python -m pytest` — **140 passed** (baseline 65 + T10/T17 additions).

performed against: FR-05 (safe-claims allowlist),
  FR-06 (Agent OBO sidecar mock boundary), FR-09 (strict JWKS validation), FR-03/FR-04 (fixtures),
  and `design.md §Security Design Notes`.

  **ADR-M5-03 decision: PyJWT + manual httpx JWKS fetch — Accepted.**
  Full decision recorded in `design.md §ADR-M5-03` and cross-cutting ADR filed at
  `.squad/architecture/decisions/003-jwks-client-caching-strategy.md`.

  **Security review findings (all addressed in spec before unblocking):**

  1. **FR-05 — `xms_act_fct` PII assessment:** APPROVED. The claim is a JSON object
     `{"appid": "<uuid>"}` — application identity metadata only; no user PII. Safe to add to
     allowlist. **CONFIRMED REQUIRED (implementation gate for T06):** `_sanitize_value()` currently
     returns `None` for `dict` inputs, which would silently drop the claim after allowlist addition.
     T06 MUST extend `_sanitize_value()` to handle `dict` type. Test assertion required. Documented
     in `design.md §Security Design Notes §3` (expanded at T03 review).

  2. **FR-06 — Agent OBO mock boundary:** APPROVED with note. ABC design, localhost enforcement,
     zero-network-calls contract, sanitized output, and audience validation order are all correct and
     safe. **Note added (Security Design Notes §10):** ABC docstrings MUST document `sanitize_claims()`
     as a binding implementation contract, not an optional choice, so future HTTP adapter implementers
     are not able to return raw claims.

  3. **FR-09 — Strict JWKS validation:**
     - Algorithm allowlist design: APPROVED. Defense-in-depth (`REJECTED_ALGORITHMS` check AND
       `ALLOWED_ALGORITHMS` membership) is correct.
     - **NEW REQUIREMENT (Security Design Notes §6):** Algorithm `alg` header MUST be lowercased
       before comparison. Mixed-case variants (`None`, `NONE`, `HS256` in wrong case) are a documented
       bypass vector. Tests for `alg:none` MUST include mixed-case variants.
     - **NEW REQUIREMENT (Security Design Notes §7):** JWKS URL MUST come exclusively from
       `AUTH_JWKS_URL` config. Token header claims `jku`/`x5u` MUST be explicitly suppressed —
       following them creates SSRF + algorithm-swap attack surface.
     - **NEW REQUIREMENT (Security Design Notes §8):** `httpx` JWKS fetch MUST bound connect + read
       timeout at ≤ 5 seconds.
     - `kid`-miss-retry strategy: APPROVED. One retry sufficient; more risks thundering herd.
     - Strict-mode fixture header suppression: APPROVED as designed.

  4. **FR-03/FR-04 — Fixture negative case coverage:** APPROVED. Seven fixtures cover the
     complete claim-level rejection vector space. The `alg:none` rejection test is correctly handled
     by crafted JWT headers in T09 (not as fixture files), consistent with FR-09 test spec.

  5. **Tracing span safety (FR-12 / Amendment 001):** APPROVED. Span attribute allowlist in
     `design.md §Required Span Attributes` excludes all PII. **NEW REQUIREMENT (Security Design
     Notes §9):** `identity_lab.fixture_name` span attribute MUST be blank/omitted in
     `AUTH_MODE=strict`. This is the tracing analogue of the fixture-header suppression rule and
     is an explicit gate for T20 (tracing security review).

  **Verdict: ACCEPTED.** All five review areas are approved (with conditions recorded as explicit
  implementation gates in `design.md §Security Design Notes §3, §6, §7, §8, §9, §10`).

  **Gate status after T03:** T01 ✅, T02 ✅, T03 ✅ — all pre-implementation gates satisfied.
  `implementation_blocked` set to `false`. Tasks T04, T06, T09, T10, T17 are now unblocked.

- 2026-05-15 — **AMENDMENT 001 APPROVED — Ashley Hollis**

  Coordinator presented the Amendment 001 checkpoint after Morpheus accepted the architecture ADRs,
  Spec Feature applied terminology/tracing revisions, and the final terminology sweep removed
  ambiguous live prose. Ashley approved the amendment. Implementation remains blocked pending
  T03 (Trinity security review + ADR-M5-03).

- 2026-05-14 — **T01 + T02 COMPLETE — Morpheus (architecture review + ADR decisions)**

  Morpheus completed T01 (spec promotion review) and T02 (ADR-M5-01 + ADR-M5-02 decisions) as part
  of the terminology/tracing amendment review session (Amendment 001).

  **T01 — Spec review findings:**
  - Spec 002 scope, ADR framing, and design decisions are architecturally sound.
  - Amendment 001 (terminology + tracing) is architecturally valid and accepted.
  - Terminology gap identified and resolved: new ADRs 001 + 002 created in
    `.squad/architecture/decisions/` with public counterparts in `docs/adr/0006` + `0007`.
  - The "local app gateway" term from the Amendment 001 spec-feature pass is superseded by
    **"Agentic Layer"** (ADR 0006 / `.squad/architecture/decisions/001-agentic-layer-vs-agent-gateway-terminology.md`)
    for consistency across all roadmap and architecture artifacts. "Standalone Agent Gateway" is
    superseded by **"AKS Agent Gateway"**. Spec 002 artifacts corrected accordingly.

  **T02 — ADR decisions recorded:**
  - ADR-M5-01: **Accepted — Option A** (standalone `environments/aks/` directory). Recorded in `design.md`.
  - ADR-M5-02: **Accepted — Option A** (in-process `AgentSidecarClient` ABC, swappable for real HTTP
    adapter). Recorded in `design.md`.

  **Cross-cutting ADRs authored (Morpheus — this session):**
  - `.squad/architecture/decisions/001-agentic-layer-vs-agent-gateway-terminology.md` — Accepted
  - `.squad/architecture/decisions/002-end-to-end-tracing-strategy.md` — Accepted
  - `docs/adr/0006-agentic-layer-vs-agent-gateway-terminology.md` — Accepted
  - `docs/adr/0007-end-to-end-tracing-strategy.md` — Accepted

  **Roadmap updated:**
  - `.squad/project/roadmap.md` — full per-milestone "Done by end of milestone / What works" detail
    added for M1–M7; cross-cutting tracing section added; terminology table added.

  **Docs updated (terminology):**
  - `docs/agent-framework/overview.md`, `python-agent.md`, `dotnet-agent.md`
  - `docs/identity/token-audience.md`, `docs/apim/ingress-policy.md`
  - `docs/architecture/07-variant-e-agent-framework.md`

  **T03 status:** Now unblocked — Trinity security review + ADR-M5-03 can proceed.
  **T17 status:** Unblocked on Morpheus side (T02 ✅); still waiting on T03 (Trinity).

- 2026-05-15 — **AMENDMENT 001 TERMINOLOGY CORRECTION — spec-feature agent**

  Terminology corrected across all Spec 002 artifacts per ADR 0006
  (`.squad/architecture/decisions/001-agentic-layer-vs-agent-gateway-terminology.md`, accepted by
  Morpheus). This is a documentation-only cleanup; implementation gate remains unchanged.

  **What changed:**
  - "local app gateway" → **Agentic Layer** everywhere it referred to `apps/agent-gateway/`
    as the lab's orchestration service.
  - "standalone Agent Gateway" → **AKS Agent Gateway** everywhere it referred to the
    agentgateway.dev infrastructure proxy.
  - Terminology tables in `README.md`, `design.md`, and `research.md` rewritten with the two
    canonical terms; retired "local app gateway" row removed.
  - Span `service.name` design values updated: `local-app-gateway` → `agentic-layer`,
    `standalone-agent-gateway` → `aks-agent-gateway`.
  - `FR-11` rewritten with canonical terms; gate checklist items updated.
  - `state.json` note added recording ADR 0006 supersession.
  - `apps/agent-gateway/` path and Docker Compose service name `agent-gateway` preserved in all
    references as the legacy filesystem/service identifier (no runtime files touched).

  **What was NOT done:**
  - No runtime, source, or Terraform files modified.
  - Implementation gate not changed — all tasks remain blocked.

- 2026-05-15 — **AMENDMENT 001 APPLIED — spec-feature agent**

  Amendment 001 sourced from `.squad/specs/002-aks-entra-agent-id/amendments/inbox/001-agentic-layer-gateway-tracing-roadmap.md`. Requested by Ashley Hollis.

  **What changed:**

  - **Terminology clarification (all artifacts):** Applied canonical terms per ADR 0006 — "Agentic Layer" for `apps/agent-gateway/` (the lab's FastAPI service); "AKS Agent Gateway" for agentgateway.dev. Retired "local app gateway" and "standalone Agent Gateway" from prose. Added Terminology section to `README.md`. Added `design.md §Terminology Definitions`. Updated AKS manifest layout wording. Added research section 8 covering naming disambiguation.

  - **End-to-end tracing requirements:** Added `requirements.md §FR-11` (terminology clarity), `§FR-12` (E2E tracing design covering span model, propagation, attributes, mock-flow infrastructure, AKS/standalone Agent Gateway integration, test isolation), and `§NFR-07` (OTEL/Jaeger standard). Added `design.md §End-to-End Tracing Design` (OTEL infrastructure, Compose overlay spec, span model diagrams for mock and AKS flows, static vs dynamic config, visualization goal). Added `research.md §8` (agentgateway.dev Jaeger/OTEL reference).

  - **New tasks T17–T20:**
    - T17 (Morpheus): Tracing design review — blocked on T02+T03
    - T18 (Neo): OTEL instrumentation for mock flow — blocked on T17
    - T19 (Tank): Docker Compose tracing overlay — blocked on T17
    - T20 (Trinity): Tracing security review — blocked on T18+T19
  - `state.json` updated: T17–T20 added as blocked; amendment record added; `implementation_blocked_reason` updated; validation commands updated.
  - `goals.md` updated: Success Criterion 10 (E2E tracing design).
  - `roadmap.md` updated: milestone outcomes clarified, M5 terminology note added.

  **What was NOT done (per task instructions):**
  - No runtime/infra code modified.
  - No Terraform or runtime files touched.
  - Roadmap updated minimally for consistency only.

  **Implementation gate:** All implementation tasks (T04–T20) remain blocked until T01 (Morpheus spec review), T02 (ADR decisions), T03 (Trinity security review), AND Amendment 001 review approval are complete.

  **Next action for coordinator:** Present Amendment 001 to Morpheus + Trinity for review gate. T01 review should include the amended artifacts.

- 2026-05-14 — **SPEC PROMOTION COMPLETE — spec-feature agent**

  Spec 002 promoted from draft/research-only to tasks-ready as M5 priority per the approved implementation plan (`plan.md`) and coordinator direction.

  **What was done:**
  - `README.md` rewritten from draft stub to full implementation-ready spec header with complete scope, artifact table, and validation targets.
  - `goals.md` created: nine success criteria covering sidecar contract, fixture set, safe-claims allowlist, Agent OBO boundary, AKS Terraform skeletons, AKS manifest docs, strict JWKS validation, ADR decisions, and public-safety constraint.
  - `research.md` created: source material, Agent ID token model, sidecar architecture (endpoints + localhost enforcement), AKS + workload identity design, strict JWKS validation research (algorithm safety, JWKS key matching, caching strategy options), and existing-state audit (shared auth library, existing fixtures, Terraform gaps, docs gaps).
  - `requirements.md` created: ten functional requirements (FR-01 through FR-10) and six non-functional requirements (NFR-01 through NFR-06) covering all ten promoted-scope items.
  - `design.md` created: three ADR candidate sections (pending decisions), full sidecar endpoint schemas with placeholder GUIDs, Agent OBO sidecar mock boundary design (ABC + MockAgentSidecarClient pseudocode), offline fixture claim content for all seven fixtures, strict JWKS validation flow, AKS Terraform skeleton layout, AKS manifest doc layout, security design notes (Trinity).
  - `tasks.md` created: sixteen tasks (T01–T16) with owners Morpheus, Tank, Trinity, Neo; dependency graph; M5 gate criteria checklist. Implementation tasks T04–T13 are explicitly blocked on review tasks T02+T03.
  - `state.json` created: machine-readable spec state, ADR tracking, task status map (all implementation tasks blocked), validation commands.
  - `.progress.md` created: artifact table, task progress table, M5 gate checklist, and this log entry.
  - Existing `identity-notes.md` and `infra-notes.md` preserved; their research content is integrated into `research.md` and `design.md`.

  **What was NOT done (per task instructions):**
  - No runtime/infra code modified.
  - No fixture JSON files created (deferred to T07 — Trinity, after T03 security review).
  - No Terraform skeletons created (deferred to T04 — Tank, after T02+T03).
  - No test files written (deferred to T08–T09/T13 after review gates).

  **Implementation gate:** T04–T16 remain blocked until T01 (Morpheus spec review), T02 (ADR decisions), and T03 (Trinity security review) are complete and recorded here.

  **Next action for coordinator:** Assign T01 to Morpheus. Schedule T01 and T03 as parallel review tasks.
