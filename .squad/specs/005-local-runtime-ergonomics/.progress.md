# Spec 005 Progress

**Spec:** 005-local-runtime-ergonomics  
**Milestone:** M4 — Local runtime ergonomics  
**Phase:** tasks-ready  
**Last updated:** 2026-05-14  

## Status

| Artifact | Status |
|----------|--------|
| `README.md` | ✅ Complete |
| `goals.md` | ✅ Complete |
| `research.md` | ✅ Complete |
| `requirements.md` | ✅ Complete |
| `design.md` | ✅ Complete |
| `tasks.md` | ✅ Complete |
| `state.json` | ✅ Complete |
| `.progress.md` | ✅ Complete |

**Spec gate:** ✅ Satisfied — Morpheus architecture review accepted and Trinity security review amended with no blockers.

## Task Progress

| Task ID | Title | Owner | Status |
|---------|-------|-------|--------|
| T01 | Wire auth env vars into base Compose | Tank | ✅ Complete |
| T02 | Add health checks to all services | Tank | ✅ Complete |
| T03 | Validate all Compose overlays | Tank | ✅ Complete |
| T04 | Update Docker/local-dev docs | Tank | ✅ Complete |
| T05 | Wire auth env vars into overlay files | Neo | ✅ Complete |
| T06 | Add `/chat/session` BFF endpoint | Neo | ✅ Complete |
| T07 | Add local CORS config to BFF | Neo | ✅ Complete |
| T08 | Add `userId` display-only guard doc/test | Neo | ✅ Complete |
| T09 | Architecture review of compose strategy | Morpheus | ✅ Complete |
| T10 | Security review of env examples | Trinity | ✅ Complete (Amended) |

## Milestone Gate Checklist

- [x] All spec artifacts complete and reviewed
- [x] Compose config checks pass for base and all variants
- [x] Env examples document offline token flow
- [x] BFF `/chat/session` contract is local-browser-compatible
- [x] Python tests remain green
- [x] No secrets or real IDs committed
- [x] Morpheus architecture sign-off
- [x] Trinity security sign-off

## Log

- 2026-05-14 — Spec 005 created by spec-feature agent per approved implementation plan.
- 2026-05-14 — **T10 SECURITY REVIEW — Trinity — AMENDED**

  **Scope:** FR-05 through FR-07, BFF browser-runtime contract (`/chat/session`, CORS, `userId` rule), and all three env example files.

  **Findings and dispositions:**

  1. **`/chat/session` response — ACCEPTED.** Response schema (`session_id` + `expires_at`) exposes no tokens and no PII. `session_id` is a server-generated UUID4 not derived from any token claim. `expires_at` is a server-generated timestamp unrelated to user identity. No raw bearer token in response. Design is sound.

  2. **CORS wildcard-with-credentials guard — AMENDED (requirements.md FR-06, design.md CORS section, tasks.md T07).** The original design's CORS snippet showed unconditional middleware registration with a static default of `["http://localhost:3000"]` but contained no runtime guard against `CORS_ALLOWED_ORIGINS=*`. Wildcard origins combined with `allow_credentials=True` is a known CORS misconfiguration. Amended: (a) FR-06 now requires a startup `RuntimeError` if `*` appears in `CORS_ALLOWED_ORIGINS`; (b) design.md now shows a conditional middleware registration block with the wildcard guard; (c) T07 adds `test_cors_wildcard_rejected_at_startup` and `test_cors_disabled_in_strict_mode_without_explicit_origins` tests.

  3. **CORS conditional activation — AMENDED (design.md).** FR-06 required CORS to be "only active in AUTH_MODE=mock or when CORS_ALLOWED_ORIGINS is explicitly set", but the design showed a static default without a mode guard — a misalignment that would cause Neo to implement CORS as always-on. Design.md now specifies the Settings loading rule: default to `["http://localhost:3000"]` only when `AUTH_MODE=mock`; default to `[]` (no middleware) when `AUTH_MODE != mock` and env var is unset.

  4. **`userId` display-only rule specification — ACCEPTED.** FR-07 correctly states the rule. The mandatory code comment is specified in design.md. T06 includes `test_chat_session_userid_is_not_authz`.

  5. **T08 test wording — AMENDED (tasks.md T08).** Original wording said "tampered/different `userId` cookie or header" — `userId` is a token claim (sub/oid), not a cookie or header. Left uncorrected, Neo would write a test checking for a non-existent header. Amended to: "two mock tokens with different `sub`/`oid` claim values but identical scp/aud/iss both succeed."

  6. **Env placeholders — ACCEPTED.** All three env example files use all-zero GUIDs (`00000000-0000-0000-0000-000000000NNN`) and `{tenant_id}` brace tokens for issuer/JWKS URLs. No real tenant IDs, client IDs, secrets, or tokens present. Public-safe.

  **Verdict: AMENDED. T06–T08 may proceed after these amendments are acknowledged by Neo.**
- 2026-05-14 — **T09 Architecture review complete (Morpheus).** Decision: ACCEPTED.

  **ADR-M4-01 (Base-plus-overlay) — Accepted.**
  Reviewed the base `docker/docker-compose.yml` (current state: no auth vars, no health checks, simple `depends_on`) against the proposed target schema in `design.md`. Option A (base carries safe offline defaults; overlays carry only diffs) is architecturally sound and consistent with Docker Compose merge-and-override semantics. Key validations:
  - The base schema in `design.md` uses only all-zero GUID placeholders — no real credentials or tenant IDs.
  - All three overlay files are already minimal in structure; the `single-tenant.yml` currently uses the variable name `ISSUER_URL` instead of `AUTH_ISSUER`. The design corrects this to `AUTH_ISSUER` and `AUTH_JWKS_URL`. **Tank (T03) and Neo (T05) MUST verify the actual variable name consumed by `config.py` in each service before applying the overlay changes.** If `config.py` reads `ISSUER_URL`, the design overlay schema must be amended to match — flag to Morpheus for sign-off if correction is required.
  - `depends_on` upgrade to `condition: service_healthy` is a correct and necessary companion to the health check additions.
  - `CORS_ALLOWED_ORIGINS` is intentionally absent from the base Compose schema; the BFF app default (`http://localhost:3000`) covers the standard local dev case, and T06 adds it to `bff.env.example`. This is acceptable.

  **ADR-M4-02 (Python stdlib health check) — Accepted.**
  The pattern `python -c "import urllib.request, sys; urllib.request.urlopen('http://localhost:8080/healthz') or sys.exit(1)"` is functionally correct:
  - Success path: `urlopen` returns a truthy response object; short-circuit prevents `sys.exit(1)`; script exits 0.
  - Failure path: `urlopen` raises `urllib.error.HTTPError` or `URLError`; uncaught exception causes Python to exit non-zero; health check fails correctly.
  - The `or sys.exit(1)` clause is benign dead code on both paths but is a harmless and widely-used pattern. No amendment required.
  - Container port `8080` is correct for all three services (`agent-gateway` maps host 8081→container 8080).
  - `start_period: 10s` is appropriate for local Python FastAPI startup.

  **Spec 005 implementation-readiness verdict:** ACCEPTED. Tank (T01–T04) and Neo (T05–T08) may begin implementation immediately after T10 (Trinity security review) completes. T01 and T05 are unblocked by T09 now; T06–T08 remain gated on T10.

  **One standing flag for implementation team:** During T03 (Tank) / T05 (Neo), verify the exact env var name for the OIDC issuer URL in each service's `config.py`. If it is `ISSUER_URL` (not `AUTH_ISSUER`), correct the overlay schema in `design.md` and re-notify Morpheus. Do not assume `AUTH_ISSUER` without confirming.
- 2026-05-14 — **T05–T08 COMPLETE — Neo**

  **T05 — Overlay env var verification findings:**

  All three service `config.py` files read `AUTH_ISSUER` and `AUTH_JWKS_URL` — confirmed aligned
  with Tank's T03 fix to `docker-compose.single-tenant.yml` (which had the wrong `ISSUER_URL` key).

  The following overlay variables are **not read by any service `config.py`** and are pass-through
  env vars only (informational flags for future use or tooling):
  - `TENANT_MODE` — set in all three overlays; not consumed by any service config.
  - `VENDOR_ID` — set in `vendor-shaped.yml`; not consumed by any service config.
  - `TENANT_A_ISSUER`, `TENANT_B_ISSUER` — set in `cross-tenant.local.yml`; not consumed.

  **No Docker file edits made** (Tank owns Docker files). Flagging to coordinator/Morpheus:
  `TENANT_MODE`, `VENDOR_ID`, `TENANT_A_ISSUER`, `TENANT_B_ISSUER` are currently inert overlay
  vars. A follow-on task (post-M4) should either wire them into service config or document them
  as reserved placeholders for M6+.

  **T06 — `/chat/session` endpoint added (`apps/bff/python-fastapi/app/main.py`):**
  - `POST /chat/session` requires delegated auth context (via `get_auth_context` dependency).
  - Returns `{"session_id": "<UUID4>", "expires_at": "<ISO-8601>"}`.
  - No raw token or PII in response.
  - Mandatory `userId` display-only comment included in implementation per design.md.
  - `CORS_ALLOWED_ORIGINS` field added to `Settings` in `config.py` with mode-based defaults.
  - `CORS_ALLOWED_ORIGINS=http://localhost:3000` added to `config/env/bff.env.example`.

  **T07 — CORS middleware added (`apps/bff/python-fastapi/app/main.py`):**
  - `CORSMiddleware` registered conditionally: only when `cors_allowed_origins` is non-empty.
  - `AUTH_MODE=mock` with no `CORS_ALLOWED_ORIGINS` env var → defaults to `["http://localhost:3000"]`.
  - Any non-mock mode with no `CORS_ALLOWED_ORIGINS` → `[]` → no middleware registered.
  - Wildcard `*` in `CORS_ALLOWED_ORIGINS` → `RuntimeError` raised at startup.
  - `allow_methods=["GET","POST"]`, `allow_headers=["Authorization","Content-Type","x-correlation-id"]`.

  **T08 — `userId` display-only rule documented and tested:**
  - Rule documented in `apps/bff/python-fastapi/README.md` (new section).
  - Code comment present in `/chat/session` implementation.
  - Tests in `tests/security/test_bff_chat_session.py` cover:
    - `test_userid_display_only_different_sub_same_auth_outcome` — claim-level proof.
    - `test_userid_display_only_via_http_both_tokens_succeed` — HTTP-level proof.

  **Validation:** `python -m pytest` — **65 passed, 0 failed** (56 pre-existing + 9 new).

  **Files changed:**
  - `apps/bff/python-fastapi/app/config.py` — added `cors_allowed_origins` to Settings.
  - `apps/bff/python-fastapi/app/main.py` — CORS middleware + `/chat/session` endpoint.
  - `apps/bff/python-fastapi/README.md` — new endpoint, userId rule, CORS docs.
  - `config/env/bff.env.example` — added `CORS_ALLOWED_ORIGINS` with explanatory comment.
  - `tests/security/test_bff_chat_session.py` — 9 new tests (T06/T07/T08).

  **M4 gate note:** T05–T08 are complete. M4 is NOT marked complete — T01–T04 (Tank) are
  done, but the gate checklist item "Env examples document offline token flow" (FR-08) is still
  open — `bff.env.example` has CORS commentary but the step-by-step offline mock token flow
  comment block specified in FR-08 is not yet present. Coordinator to assess and assign.



  **T03 config.py issuer var verification (resolves Morpheus standing flag):**
  All three `config.py` files (`bff`, `agent-gateway`, `mcp-protected-api`) read `AUTH_ISSUER` and `AUTH_JWKS_URL`. The existing `docker-compose.single-tenant.yml` used the incorrect variable name `ISSUER_URL` (which the services do not read). This has been corrected to `AUTH_ISSUER` + `AUTH_JWKS_URL` for all three services in the single-tenant overlay. No amendment to `design.md` required — the design already specified the correct names.

  **Changes made:**
  - `docker/docker-compose.yml`: Added `AUTH_MODE=mock`, `AUTH_FIXTURE`, `ALLOWED_AUDIENCES`, `REQUIRED_SCOPES`, `TRUSTED_TENANTS`, `CORRELATION_HEADER`, `ENABLE_DEBUG_CLAIMS` to all three services. Added `OBO_DOWNSTREAM_AUDIENCE` and `OBO_REQUIRED_SCOPES` to `agent-gateway`. Added Python-stdlib `healthcheck` blocks to all three services. Upgraded `bff.depends_on` to `condition: service_healthy`.
  - `docker/docker-compose.single-tenant.yml`: Replaced wrong `ISSUER_URL` with `AUTH_ISSUER` + `AUTH_JWKS_URL`; added both vars to `agent-gateway` and `mcp-protected-api` (previously missing).
  - `docker/README.md`: Full rewrite — base-plus-overlay strategy, mock auth defaults, startup commands for all variants, health verification, env-file vs inline note, strict-mode/M6 note.
  - `.squad/specs/005-local-runtime-ergonomics/.progress.md`: T01–T04 marked complete; gate checklist updated.

  **Validation:** All four `docker compose … config --quiet` combinations passed with exit code 0.

  **Handoff to Neo (T05–T08):**
  - T05: Overlay files are clean. `docker-compose.vendor-shaped.yml` and `docker-compose.cross-tenant.local.yml` were already minimal and correct — no changes needed. `TENANT_MODE`, `VENDOR_ID`, `TENANT_A_ISSUER`, `TENANT_B_ISSUER` are present as placeholders; Neo should confirm which of these are consumed by `config.py` and flag any missing config keys per T05 instructions.
  - T06–T08: BFF `/chat/session`, CORS, and `userId` rule are Neo's stream. The base Compose already sets `AUTH_MODE=mock` so the BFF will start in mock mode for local dev.

- 2026-05-14 — **FR-08 COMPLETE — Tank (follow-up)**

  **Scope:** Env example commentary — offline mock token flow step-by-step block (FR-08).

  The gate checklist item "Env examples document offline token flow" was the sole remaining open
  M4 gate item after T01–T08 completed. Neo noted it in the T05–T08 handoff.

  **Changes made:**
  - `config/env/bff.env.example` — Added 9-line FR-08 comment block at top: four-step offline
    mock token flow (AUTH_MODE=mock, AUTH_FIXTURE/header, {tenant_id} placeholder rule, all-zero
    GUID safety note). Existing inline comments retained.
  - `config/env/agent-gateway.env.example` — Same FR-08 comment block added. Also added
    `OBO_DOWNSTREAM_AUDIENCE` and `OBO_REQUIRED_SCOPES` placeholder vars (already present in
    base Compose but absent from the example file) with explanatory comment on mock vs strict
    behaviour.
  - `config/env/mcp-protected-api.env.example` — Same FR-08 comment block added.
  - `.squad/specs/005-local-runtime-ergonomics/.progress.md` — Gate item checked; this log entry
    appended.

  **No real tenant IDs, secrets, client IDs, or tokens introduced.** All GUIDs remain all-zero
  placeholders. No runtime code touched; no tests required per task instructions.

  **M4 gate note:** All gate checklist items are now checked. Coordinator to perform final
  validation and roadmap closure.

