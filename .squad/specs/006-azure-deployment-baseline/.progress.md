# Spec 006 Progress

**Spec:** 006-azure-deployment-baseline
**Milestone:** M6 -- Azure deployment baseline
**Phase:** implementation (all review gates satisfied)
**Last updated:** 2026-06-01

## Status

| Artifact | Status |
|----------|--------|
| README.md | complete |
| goals.md | complete |
| research.md | complete |
| requirements.md | complete |
| design.md | complete |
| tasks.md | complete |
| state.json | complete |
| .progress.md | complete |

**Spec gate:** T11 (Morpheus) ACCEPTED 2026-06-01. T12 (Trinity) ACCEPTED 2026-06-01. T00 complete. All gates satisfied.

**Milestone status:** ALL REVIEW GATES CLEAR. T01-T10 implementation unblocked.

## Task Progress

| Task ID | Title | Owner | Status |
|---------|-------|-------|--------|
| T00 | Rename agent-gateway to agent-execution (M6 Task 0) | Neo | Complete (229/229 tests pass; apps/agent-execution/ exists) |
| T11 | Architecture review | Morpheus | Complete -- ACCEPTED 2026-06-01 |
| T12 | Security review | Trinity | Complete -- ACCEPTED 2026-06-01 |
| T01 | Create single-tenant-aca Terraform environment | Tank | Complete |
| T02 | Create app-insights Terraform module skeleton | Tank | Complete |
| T03 | Wire ACA container app modules for all three services | Tank | Complete |
| T04 | Wire APIM module for ACA backend | Tank | Complete |
| T05 | Wire managed identity module per container app | Tank | Complete |
| T06 | Create docker-compose.strict-aca.yml overlay | Tank | Complete |
| T07 | CI validation gates and no-secret scan | Tank | Complete |
| T08 | Update deployment documentation | Tank | Complete |
| T09 | AUTH_MODE=strict enforcement verification in Python apps | Neo | Complete (235/235 tests pass; T12 C1 guard added to agent-execution config) |
| T10 | Azure Monitor OTLP endpoint config and env examples | Neo | Complete (env examples + azure-container-apps.md updated; no PII/token span attrs) |

## Milestone Gate Checklist

- [x] T00 complete -- apps/agent-execution/ exists; 229/229 tests pass; compose config passes
- [x] T11 complete -- Morpheus architecture review ACCEPTED; ADR-M6-01 and ADR-M6-02 accepted
- [x] T12 complete -- Trinity security review ACCEPTED; ADR-M6-03 accepted
- [x] ACA Terraform environment scaffolded and terraform fmt/validate passes
- [x] APIM + managed identity wired in skeleton
- [x] No real tenant/subscription/client IDs or secrets committed
- [x] AUTH_MODE=strict enforcement verified in Python apps
- [x] Azure Monitor OTLP endpoint env vars documented in .env.example files
- [x] docker compose strict-aca overlay config validates
- [x] CI validation gates pass (fmt, validate, compose config, no-secret scan)
- [x] python -m pytest passes with no regressions (235/235 tests pass — T09 log confirmed)
- [x] Deployment documentation updated

## Log

- 2026-06-01 -- Spec 006 scaffold created by Tank per M6 spec-first gate and Ashley Hollis directive. All spec artifacts created. Implementation blocked pending T11 (Morpheus) and T12 (Trinity) review sign-offs and T00 (rename). Coordinator approval checkpoint required before any T01-T10 implementation begins.

- 2026-06-01 -- T00 COMPLETE (Neo). apps/agent-gateway/ renamed to apps/agent-execution/; Docker Compose service renamed to agent-execution; all Python imports and CI references updated. Validation: 229/229 pytest pass; all Compose config --quiet checks pass; terraform fmt -check -recursive passes.

- 2026-06-01 -- T12 COMPLETE -- SECURITY SIGN-OFF (Trinity). ADR-M6-03 ACCEPTED -- Option A (user-assigned managed identity per service). Binding conditions: C1 -- add blueprint_audience strict-mode validation in T09; C2 -- APIM must not substitute delegated token with managed identity token in T04. T06 and T09 unblocked.

- 2026-06-01 -- T09 COMPLETE (Neo). AUTH_MODE=strict enforcement verified. Added post-validate_strict_config blueprint_audience placeholder guard in apps/agent-execution/python-fastapi-agent-framework/app/config.py (T12 binding condition C1). 6 new tests added in tests/security/test_t09_blueprint_audience_strict.py. 235/235 tests pass; docker compose config --quiet passes.

- 2026-06-01 -- T10 COMPLETE (Neo). Azure Monitor OTLP endpoint env var documentation added to config/env/agent-execution.env.example, config/env/bff.env.example, config/env/mcp-protected-api.env.example, and docs/deployment/azure-container-apps.md. No hardcoded secrets, no PII/token span attributes. ADR-M6-02 OTLP Option A documented. Full review of Spec 006 design.md, requirements.md, tasks.md, research.md, and all existing Terraform module stubs performed. Verdict: ACCEPTED. Design is architecturally coherent and implementation-ready for the Azure deployment baseline.

  ADR-M6-01 (ACA Default Path): ACCEPTED -- Option A. ACA is the M6 default deployment path. infra/terraform/environments/single-tenant-aca/ is the correct new environment. AKS skeletons from M5 preserved unchanged (NFR-06). ADR filed at .squad/architecture/decisions/005-aca-default-deployment-path.md.

  ADR-M6-02 (Azure Monitor OTLP): ACCEPTED -- Option A. OTLP endpoint env var swap. Zero code change from M5 identity_lab_auth/telemetry.py. APPLICATIONINSIGHTS_CONNECTION_STRING deferred to post-M6. ADR filed at .squad/architecture/decisions/006-azure-monitor-otlp-endpoint.md.

  ADR-M6-03 (Managed identity strategy): Architecture-level recommendation is user-assigned managed identity per service (Option A). Decision confirmed by Trinity at T12.

  Open questions resolved:
  - Q1 (ACA vs AKS default): ACA default, AKS optional. (ADR-M6-01)
  - Q2 (OTLP vs connection string): OTLP Option A. (ADR-M6-02)
  - Q3 (user-assigned vs system-assigned MI): User-assigned per service. (ADR-M6-03, Trinity T12)
  - Q4 (k8s-bootstrap namespace inconsistency): Defer to post-M6. AKS path orthogonal to M6.
  - Q5 (APIM MI role assignment scope): Resource group scope. (Trinity T12)
  - Q6 (container app ingress): Scaffold uses external_enabled = var.external_enabled (default false). T08 must document that live deployment requires external_enabled = true on BFF or APIM VNet injection.

  Implementation notes for Tank (binding):
  - log-analytics module: add workspace_id output (T01/T02)
  - container-apps-env module: add environment_id output (T01)
  - managed-identity module: add identity_id and principal_id outputs (T05)
  - container-app module: add fqdn and identity_id outputs (T03)
  - apim module: add backend_url input variable (T04 -- currently missing from variables.tf)
  - AUTH_MODE must be hardcoded to strict in the container-app module template -- not a variable (T03)
  - T02 must be completed before or in parallel with T01 (app-insights module referenced by single-tenant-aca/main.tf)

- 2026-06-01 -- T01-T08 COMPLETE (Tank). M6 Azure deployment baseline infrastructure implemented.
  Files created/updated:
  - infra/terraform/modules/app-insights/{main,variables,outputs}.tf (T02)
  - infra/terraform/modules/log-analytics/{main,outputs}.tf — workspace_id output added (T01)
  - infra/terraform/modules/container-apps-env/{main,variables,outputs}.tf — environment_id output, log_analytics_workspace_id var (T01)
  - infra/terraform/modules/managed-identity/{main,outputs}.tf — identity_id and principal_id outputs (T05)
  - infra/terraform/modules/container-app/{main,variables,outputs}.tf — full resource body, AUTH_MODE=strict hardcoded, fqdn output (T03)
  - infra/terraform/modules/apim/{main,variables,outputs}.tf — backend_url var, SystemAssigned identity, azurerm_api_management_backend, C2 comment (T04)
  - infra/terraform/policies/apim/ingress-validate-user-token.xml — explicit C2 security binding comment added (T04)
  - infra/terraform/environments/single-tenant-aca/{main,variables,outputs,providers}.tf + terraform.tfvars.example (T01)
  - docker/docker-compose.strict-aca.yml (T06)
  - docs/deployment/aca/README.md (T08)
  Validation results:
  - terraform fmt -check -recursive: PASS
  - terraform init -backend=false (single-tenant-aca): PASS (azurerm 3.117.1, azuread 2.53.1)
  - terraform validate (single-tenant-aca): PASS ("The configuration is valid.")
  - docker compose -f docker-compose.yml -f docker-compose.strict-aca.yml config --quiet: PASS
  - docker compose -f docker-compose.yml -f docker-compose.tracing.yml config --quiet: PASS
  - No-secret scan (M6 files): PASS (no real GUIDs found)

  STATUS: ALL review gates satisfied. T01-T10 FULLY UNBLOCKED.

- 2026-06-01 — POST-IMPLEMENTATION ARCHITECTURE SIGN-OFF (Morpheus). VERDICT: ACCEPTED. M6 implementation matches Spec 006 design and all three ADRs. M6 may proceed to Trinity final closure review.

  Review findings:

  1. ACA topology — CONFIRMED. APIM → BFF → Agent Execution Service → MCP Protected API wired correctly. APIM backend_url constructed as `"https://${module.container_app_bff.fqdn}"` from the container_app_bff.fqdn output. Routing chain is coherent and matches design.md topology diagram.

  2. Terraform module interfaces — CONFIRMED. All Morpheus binding notes from T11 satisfied:
     - log-analytics: `workspace_id` output present and wired to app-insights + container-apps-env.
     - container-apps-env: `environment_id` output present and wired to all three container-app modules.
     - managed-identity: `identity_id` and `principal_id` outputs present; principal_id surfaced in environment outputs.
     - container-app: `fqdn` and `identity_id` outputs present; fqdn consumed by APIM backend_url construction.
     - apim: `backend_url` input variable present; `gateway_url` output correct.
     Output names are more descriptive than the FR-02 minimum spec (`bff_fqdn` vs `bff_url`); this is a coherent design evolution, not a regression. terraform validate passes.

  3. APIM backend_url — CONFIRMED. `azurerm_api_management_backend.bff` resource present in apim/main.tf; `url = var.backend_url`; wired in single-tenant-aca/main.tf as `backend_url = "https://${module.container_app_bff.fqdn}"`. C2 security binding comment present in both apim/main.tf and ingress-validate-user-token.xml. Delegated user token passthrough is explicit.

  4. Managed identity decomposition — CONFIRMED. Three separate azurerm_user_assigned_identity resources (managed_identity_bff, managed_identity_agent_execution, managed_identity_mcp_protected_api). APIM uses SystemAssigned for its own identity and is explicitly prohibited from injecting MI token in Authorization header (C2 binding in Terraform and policy XML). OBO boundary correctly scoped only to agent_execution identity. Role assignment placeholder comments present in main.tf. Matches ADR-M6-03 / ADR-005.

  5. Azure Monitor / App Insights OTLP — CONFIRMED. app-insights module uses workspace-based App Insights (`workspace_id` wired, `application_type = "web"`). Outputs instrumentation_key and connection_string marked sensitive. OTLP endpoint is config-only: injected as `OTEL_EXPORTER_OTLP_ENDPOINT` env var in container-app template and in docker-compose.strict-aca.yml. No code change from M5 telemetry.py. Local Jaeger path undisturbed.

  6. Agent Execution Service naming — CONFIRMED. Slug `agent-execution` used consistently: Terraform resource name `*-agent-execution`, managed identity `*-id-agent-execution`, Compose service `agent-execution`, OTEL_SERVICE_NAME `agent-execution`, docs/deployment/aca/README.md, ADR-M6-01/02/03 files. No legacy `agent-gateway` references in M6 surfaces.

  7. Public-safe placeholders — CONFIRMED. terraform.tfvars.example uses `{subscription-id}` and `{tenant-id}` brace tokens only. Policy XML uses `{tenant-id-placeholder}`. All-zero GUIDs used in audience/issuer placeholders. No real tenant IDs, subscription IDs, client IDs, or secrets in any committed file. NFR-01 and NFR-05 satisfied.

  8. Spec 006 state/progress — CORRECTED. Fixed unchecked pytest checkbox (235/235 tests confirmed). state.json updated to reflect post-implementation architecture sign-off. One pre-existing stale note: roadmap.md still shows M6 as "Spec-ready" — corrected in state.json; roadmap update deferred to Trinity closure review.

  No implementation changes required. Architecture is sound for M6 closure.
