# Spec 007 Progress

**Spec:** 007-variant-client-implementations
**Milestone:** M7 — Variant client implementations
**Phase:** closed
**Last updated:** 2026-06-02

---

## Status

| Artifact | Status |
|----------|--------|
| README.md | ✅ complete |
| goals.md | ✅ complete |
| research.md | ✅ complete |
| requirements.md | ✅ complete |
| design.md | ✅ complete |
| tasks.md | ✅ complete |
| state.json | ✅ complete |
| .progress.md | ✅ complete |

**Spec gate:** ✅ All eight spec artifacts complete.

**Implementation gate:** ✅ COMPLETE — T11/T12 accepted, implementation tasks T00–T08 complete, and post-implementation reviews T09/T10 accepted.

---

## Task Progress

| Task ID | Title | Owner | Status |
|---------|-------|-------|--------|
| T11 | Architecture review | Morpheus | ✅ Complete — ACCEPTED (2026-06-01) |
| T12 | Security review | Trinity | ✅ Complete — ACCEPTED with binding conditions (2026-06-01) |
| T00 | BFF CORS + env example update | Neo | ✅ Complete (reconciled from Neo validation run; 239 pytest passing) |
| T01 | BFF `/chat/session` body model | Neo | ✅ Complete (reconciled from Neo validation run; 239 pytest passing) |
| T02 | SPA / Public Client implementation | Mouse | ✅ Complete (2026-06-01) |
| T03 | SharePoint classic loader implementation | Mouse | ✅ Complete (pluggable SharePoint/mock token providers + traceparent + lint/test validation) |
| T04 | SPFx web part implementation | Mouse | ✅ Complete (minimal scaffold + AadHttpClient + traceparent + build validation) |
| T05 | Tracing validation across variants | Mouse | ✅ Complete (SPA/classic traceparent tests verified; SPFx static traceparent guard + docs updated) |
| T06 | Diagrams and architecture doc updates | Mouse | ✅ Complete (M7 token/BFF flow diagrams + architecture notes updated) |
| T07 | Negative tests | Mouse | ✅ Complete (2026-06-01; BC-03/05/06/07 security regressions validated) |
| T08 | CI validation gates | Mouse/Neo | ✅ Complete (2026-06-01; python + SPA/classic/SPFx + compose + public-safe checks + CI gate wiring) |
| T09 | Morpheus post-implementation review | Morpheus | ✅ Complete — ACCEPTED (2026-06-02; A-01 remediation verified) |
| T10 | Trinity post-implementation security review | Trinity | ✅ Complete — ACCEPTED (2026-06-02; A-01 remediation verified, no remaining T10 blockers) |
| T13 | Final closeout | Mouse | ✅ Complete (2026-06-02; M7 closed after T09/T10 acceptance) |

---

## Milestone Gate Checklist

- [x] Spec 007 directory created: `.squad/specs/007-variant-client-implementations/`
- [x] All eight spec artifacts complete
- [x] Coordinator approval received (Ashley Hollis)
- [x] T11 complete — Morpheus architecture review sign-off
- [x] T12 complete — Trinity security review sign-off
- [x] T00 complete — BFF CORS + env example update
- [x] T01 complete — BFF body model (if T11 approves display_name)
- [x] T02 complete — SPA/public client builds and lints clean
- [x] T03 complete — Classic loader lints clean, pluggable provider implemented
- [x] T04 complete — SPFx web part builds
- [x] T05 complete — traceparent propagation verified
- [x] T06 complete — diagrams and architecture docs updated
- [x] T07 complete — negative tests pass
- [x] T08 complete — CI validation gates pass
- [x] T09 complete — Morpheus post-implementation review ACCEPTED (2026-06-02 re-review)
- [x] T10 complete — Trinity post-implementation security review ACCEPTED (2026-06-02 re-review)
- [x] python -m pytest passes (235+ tests) — no regressions
- [x] No real tenant/client IDs/secrets committed
- [x] roadmap.md updated: M7 → ✅ Complete; M8 retained as opt-in live Azure E2E gate
- [x] T13 complete — spec closed

---

## Open Questions

| # | Question | Owner | Status |
|---|----------|-------|--------|
| OQ1 | Should `/chat/session` accept an optional `display_name` body field? | Morpheus (T11) | ✅ Closed — ACCEPTED with conditions. See ADR-M7-04 and `docs/adr/0009-m7-display-name-bff-body-field.md`. |
| OQ2 | SPFx token provider: `AadHttpClient` vs `AadTokenProvider`? | Trinity (T12) | ✅ Closed — `AadHttpClient` CONFIRMED. See ADR-M7-05. |
| OQ3 | MSAL `cacheLocation`: confirm `sessionStorage` is acceptable? | Trinity (T12) | ✅ Closed — `sessionStorage` CONFIRMED. ADR-M7-01 accepted. |
| OQ4 | Classic loader `traceparent`: manual construction vs OTEL SDK? | Mouse | ✅ Closed — Manual construction. Minimal dependency requirement confirmed.
| OQ5 | M7-AzureVerify: named as M7 sub-gate or M8? | Coordinator (Ashley) | ✅ Closed — M8 per roadmap.md. ADR-M7-03 confirmed. |

---

## Log

- 2026-06-02 — **T13 COMPLETE — FINAL CLOSEOUT** (Mouse, Frontend/SharePoint/TypeScript). Reconciled final M7 status after A-01 remediation lifecycle: T09 re-review ACCEPTED by Morpheus and T10 re-review ACCEPTED by Trinity. Spec state closed (`phase: closed`, `status: complete`), task status finalized (`T00–T10 complete/accepted`, `T13 complete`), roadmap updated to mark M7 complete with validation summary, and current focus advanced to M8 opt-in live Azure E2E planning. Final closeout validation performed: Spec 007 `state.json` parsed successfully and status checks confirm **M7 complete / M8 next** posture with no live Azure execution in M7.
- 2026-06-01 — Spec 007 created by Mouse (Frontend/SharePoint/TypeScript). All eight spec artifacts created. Implementation blocked pending coordinator approval and T11 (Morpheus) + T12 (Trinity) review sign-offs.
- 2026-06-01 — **T11 COMPLETE — ACCEPTED** (Morpheus, Lead/Architect). All seven focus areas reviewed. Verdict: ACCEPTED with implementation conditions. Decisions recorded:
  - ADR-M7-01 (sessionStorage): Confirmed.
  - ADR-M7-02 (pluggable callback): Confirmed.
  - ADR-M7-03 (M8 gate): Confirmed. OQ5 closed — gate is M8, not M7-AzureVerify sub-gate.
  - ADR-M7-04 (display_name): ACCEPTED. OQ1 closed. Formal ADR at `docs/adr/0009-m7-display-name-bff-body-field.md`.
  - **Implementation condition (T00):** Add `traceparent` to BFF CORS `allow_headers` — required for browser W3C trace header propagation.
  - **Implementation condition (T01):** `display_name` must NOT be echoed in session response; negative test required; max-length guard recommended.
  - **Implementation condition (T03):** classic loader must not pass `userId` to BFF as auth signal; identity invariant comment required at call site.
  - Implementation may proceed for T00/T01 after T12 sign-off. T02–T04 require both T11 + T12.
- 2026-06-01 — **T12 COMPLETE — ACCEPTED** (Trinity, Identity & Security Engineer). All nine security focus areas reviewed. Spec 007 is security-sound at design level. OQ2 and OQ3 resolved. ADR-M7-05 created. B-02 fixed (spa.env.example updated). Binding conditions recorded below. **M7 implementation may proceed.**
- 2026-06-01 — **T04 COMPLETE** (Mouse, Frontend/SharePoint/TypeScript). Implemented minimal SPFx web part scaffold in `apps/spfx-webpart/identity-chat-webpart/` with `AadHttpClient` via `context.aadHttpClientFactory.getClient(bffResourceUri)`, manual `traceparent` forwarding, `webApiPermissionRequests` constrained to BFF placeholder `api://{client-id}` + `access_as_user`, and identity invariant comment at BFF call site. Validation run: `npm ci && npm run build` passed (build uses `gulp build` + TypeScript compile). No raw-token persistence APIs added.
- 2026-06-01 — **T03 COMPLETE** (Mouse, Frontend/SharePoint/TypeScript). Replaced classic placeholder with minimal implementation in `apps/sharepoint-classic/chat-loader-js/`: pluggable token provider interface, default `_spPageContextInfo.aadTokenProviderFactory` provider, and local/mock stub provider; manual W3C `traceparent`; BFF `POST /chat/session` call with `Authorization`, `traceparent`, and JSON body containing optional `display_name` only; explicit identity invariant at call site (`display_name` display-only, never auth); no token persistence to storage APIs. Added `config.example.json` placeholders, lint/test tooling (`package.json`, `.eslintrc.json`, `chat-loader.test.js`), and updated `sample-aspx-snippets/classic-page-snippet.html` plus loader README with local/mock + no-live-Azure-in-M7 guidance. Validation run: `npm ci && npm run lint && npm test` passed.
- 2026-06-01 — **T02 COMPLETE** (Mouse, Frontend/SharePoint/TypeScript). Replaced the React/Vite placeholder in `apps/spa-public-client/react-vite/` with a minimal public-client implementation using MSAL Browser Auth Code + PKCE, `cacheLocation: 'sessionStorage'`, and `storeAuthStateInCookie: false`. Added BFF-only request wrapper with `Authorization: Bearer` and `traceparent` headers, plus identity-invariant call-site comment clarifying that `userId`/`display_name` are display-only and never identity. Added public-safe `.env.example` placeholders (`{tenant-id}`, `{client-id}`, `{bff-api-scope}`), README security/run guidance, and unit tests for MSAL cache policy and BFF traceparent/auth headers. Validation run: `npm ci && npm run build && npm run lint && npm test` passed (3 tests).
- 2026-06-01 — **T05 COMPLETE** (Mouse, Frontend/SharePoint/TypeScript). Verified tracing validation across variants: SPA unit test asserts `traceparent` on BFF calls (`src/bffClient.test.ts`), classic loader unit test asserts `traceparent` forwarding (`chat-loader.test.js`), and SPFx now has a static/unit guard (`tests/traceparent-forwarding.test.js`) confirming `traceparent` is passed in `AadHttpClient` request options. Added local Docker Compose + Jaeger tracing walkthrough and BC-07 telemetry safety rules to SPA/classic/SPFx READMEs (explicitly banning raw token, `oid`, `sub`, `email`, `upn`, `preferred_username`, and `tracestate` leakage).
- 2026-06-01 — **Reconciliation update** (Mouse): task state corrected to reflect Neo-completed backend prerequisites. T00 and T01 marked complete per reported Neo execution with 239 pytest passing.
- 2026-06-01 — **T06 COMPLETE** (Mouse, Frontend/SharePoint/TypeScript). Updated `diagrams/mermaid/variant-a-sharepoint-classic.mmd`, `variant-b-spfx.mmd`, and `variant-d-spa-comparison.mmd` to reflect implemented M7 token acquisition + BFF handoff + traceparent forwarding. Updated architecture docs `03-variant-a-sharepoint-classic.md`, `04-variant-b-spfx.md`, and `06-variant-d-spa-comparison.md` with implementation notes covering BFF-only boundary, `userId`/`display_name` display-only invariant, local/mock scope, M8 Azure E2E deferral, and canonical Agent Execution Service terminology.
- 2026-06-01 — **T07 COMPLETE** (Mouse, Trinity security focus). Added negative/security regression coverage across variants: SPA tests now assert MSAL `sessionStorage` and reject 401 paths when bearer token is unusable; classic loader tests assert no storage persistence writes and verify no `userId` auth signal is emitted; BFF `/chat/session` tests now assert 401 when `userId` is supplied without auth. Added static security regression suite `tests/security/test_m7_variant_identity_boundaries.py` validating SPFx `AadHttpClient` usage, no `AadTokenProvider`/`SPHttpClient` BFF path, no storage writes, placeholder-only `webApiPermissionRequests` (T-SEC-12), and no PII/token trace/log snippets in variant source. Added T-SEC-11 regression in `test_jwks_strict.py` proving `alg:none`/`HS*` rejection remains enforced with fixture-derived payloads. Validation run: `python -m pytest` (246 passed), SPA `npm test` (4 passed), classic loader `npm test` (4 passed), SPFx `npm ci && npm run build` (pass, Node engine warnings only).
- 2026-06-01 — **T08 COMPLETE** (Neo, Python/Agent backend). Executed validation gates locally: `python -m pytest` (246 passed), SPA `npm ci && npm run build && npm run lint && npm test` (pass), classic `npm ci && npm run lint && npm test` (pass), SPFx `npm ci && npm run build && npm test` (pass), and `docker compose -f docker/docker-compose.yml config --quiet` (pass). Updated `.github/workflows/ci.yml` to include Python + SPA/classic/SPFx validations, compose `config --quiet`, and public-safe checks for placeholders/no `.default` in SPA examples/`webApiPermissionRequests` BFF scope only. Confirmed `LIVE_AZURE_TESTS` is not defaulted in CI.
- 2026-06-01 — **T10 REVIEW — REJECTED** (Trinity, Identity & Security Engineer). Blocking finding **T10-01**: `apps/spfx-webpart/identity-chat-webpart/src/webparts/identityChat/IdentityChatWebPart.ts` still sends `userId` in the `POST /chat/session` request body (`userId: this.context.pageContext.user?.displayName`). This exceeds the approved ADR 0009 contract, which allows only optional `display_name`, and creates an avoidable identity-boundary regression risk even though the BFF currently ignores unknown fields. Focused validation performed during review: `python -m pytest tests/security/test_bff_chat_session.py tests/security/test_m7_variant_identity_boundaries.py tests/security/test_jwks_strict.py tests/security/test_telemetry.py` (91 passed), `apps/sharepoint-classic/chat-loader-js` `npm test` (4 passed), and `apps/spfx-webpart/identity-chat-webpart` `npm test` (1 passed). SPA `npm test -- --runInBand` was attempted but Vitest rejected the unsupported flag; this did not affect the blocking finding. **Recommended revision owner:** Neo (not original author), to remove `userId` from the SPFx request body and add a regression assertion that the SPFx BFF payload contains no `userId` field.
- 2026-06-01 — **T09 REVIEW — REJECTED** (Morpheus, Lead/Architect). Blocking finding A-01: `apps/spfx-webpart/identity-chat-webpart/src/webparts/identityChat/IdentityChatWebPart.ts` sends `userId` in the `/chat/session` request body alongside `display_name`. This drifts from the approved M7 contract, where ADR 0009 and the BFF request model authorize only the minimal optional `display_name` body extension. Although the BFF currently ignores extra fields and auth still anchors on the bearer token, the SPFx client is now depending on a non-spec field, which weakens the "minimal, backward-compatible" contract boundary and leaves T07/T08 without a guard against future regression. Recommended revision owner (per rejection protocol, not original author): **Neo**. Required revision: remove `userId` from the SPFx request body, keep the identity-invariant comment, and add/extend a regression check proving SPFx sends `display_name` only.
- 2026-06-02 — **T09 A-01 REMEDIATION APPLIED** (Neo, Python/Agent backend revision owner). Updated SPFx `IdentityChatWebPart.ts` so `/chat/session` no longer sends `userId`; request now sends optional `display_name` only, and omits the body entirely when a display name is unavailable. Preserved the identity-invariant comment clarifying display hints are never identity and bearer token validation at the BFF remains the trust anchor. Added regression guards in `apps/spfx-webpart/identity-chat-webpart/tests/traceparent-forwarding.test.js` and `tests/security/test_m7_variant_identity_boundaries.py` to prevent `userId` from reappearing in SPFx `/chat/session` calls. **T09 remains pending Morpheus re-review; not accepted.**
- 2026-06-02 — **T09 RE-REVIEW — ACCEPTED** (Morpheus, Lead/Architect). Re-checked A-01 remediation against ADR 0009, the BFF `/chat/session` request model, and regression coverage. Verified SPFx no longer sends `userId`, sends only optional `display_name` when present and omits the body otherwise, and retains the identity-invariant comment at the BFF call site. Confirmed the BFF contract remains optional `display_name` only and focused regression validation passed: `python -m pytest tests/security/test_m7_variant_identity_boundaries.py tests/security/test_bff_chat_session.py` (19 passed) and `cd apps/spfx-webpart/identity-chat-webpart && npm run build && npm test` (pass). A-01 is closed and T09 architecture gate is now ACCEPTED.
- 2026-06-02 — **T10 RE-REVIEW — ACCEPTED** (Trinity, Identity & Security Engineer). Re-checked the A-01 SPFx remediation against ADR 0009, the BFF `/chat/session` contract, telemetry/JWKS safeguards, and T12 binding conditions. Verified SPFx no longer sends `userId`, sends only optional `display_name` when present and omits the body otherwise, preserves the identity-invariant comment that keeps `display_name` display-only, continues using `AadHttpClient` with BFF placeholder-only delegated scope, and does not introduce token/PII logging or tracing regressions. Focused validation passed: `python -m pytest tests/security/test_bff_chat_session.py tests/security/test_m7_variant_identity_boundaries.py tests/security/test_jwks_strict.py tests/security/test_telemetry.py` (91 passed) and `cd apps/spfx-webpart/identity-chat-webpart && npm run build && npm test` (pass). T10-01 is closed and the T10 security gate is now ACCEPTED.

---

## T12 Security Review — Binding Conditions

> These conditions are non-negotiable. All must be satisfied before token acquisition code (T02–T04) ships. Violation of any condition is a blocker for T10 (Trinity post-implementation review).

| ID | Condition | Owner | Verified by |
|----|-----------|-------|------------|
| BC-01 | `spa.env.example` uses all-zero GUID placeholders for tenant/client IDs and explicit delegated scope (no `.default`). ✅ Fixed in T12. | Trinity | T08 no-secret scan |
| BC-02 | ADR-M7-05 (`AadHttpClient` for SPFx, OQ2) recorded in `design.md`. ✅ Done in T12. | Trinity | T10 review |
| BC-03 | T07 task scope MUST explicitly include CORS preflight tests T-SEC-08 (allowed origin) and T-SEC-09 (disallowed origin). Currently absent from tasks.md T07. Neo owns as part of T00. | Neo | T07 / T10 |
| BC-04 | `bff.env.example` CORS_ALLOWED_ORIGINS for SPFx local dev MUST include `https://localhost:4321` (SPFx workbench uses HTTPS locally). Add to T00 scope. | Neo | T00 |
| BC-05 | T07 must verify `alg:none` / `HS*` token rejection (T-SEC-11) is still covered for new variant fixture paths. Check against existing M1 baseline tests. | Neo/Mouse | T07 |
| BC-06 | `package-solution.json` `webApiPermissionRequests` MUST use placeholder `{client-id}` brace tokens and BFF scope only. No over-broad Graph scopes. T-SEC-12 static check required in T08 CI. | Mouse | T07 / T08 |
| BC-07 | No raw token, `oid`, `sub`, `email`, `upn`, or `preferred_username` in any span attribute or structured log from any variant. Applies to W3C `tracestate` and all custom OTEL attributes too. | Mouse/Neo | T05 / T10 |
| BC-08 | `window.location.origin` in SPA redirect URI is acceptable for runtime use but all deployment origins MUST be explicitly pre-registered in the Entra app registration. Wildcard redirect URIs are prohibited. | Mouse | T08 / T10 |
| BC-09 | `.env.live.example` pattern (B-05) is deferred to M8 gate. Not required for M7. M8 must not proceed without it. | Tank/Neo | M8 gate |
