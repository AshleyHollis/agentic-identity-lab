# Spec 008 Progress

**Spec:** 008-live-azure-e2e-gate  
**Milestone:** M8 — Live Azure E2E gate  
**Phase:** closed  
**Last updated:** 2026-05-10

---

## Status

| Artifact | Status |
|---|---|
| README.md | ✅ complete |
| goals.md | ✅ complete |
| research.md | ✅ complete |
| requirements.md | ✅ complete |
| design.md | ✅ complete |
| tasks.md | ✅ complete |
| state.json | ✅ complete |
| .progress.md | ✅ complete |

**Spec gate:** ✅ T10, T11, and T12 are accepted. Spec 008 is implementation-ready at the planning gate.

**Implementation gate:** ✅ Closed — T15 final closeout completed. M8 implementation scaffolds are complete and public-safe.

---

## Task Progress

| Task ID | Title | Owner | Status |
|---|---|---|---|
| T10 | Tank deployment review | Tank | ✅ Complete — ACCEPTED (2026-05-10 re-review) |
| T11 | Morpheus architecture review | Morpheus | ✅ Complete — ACCEPTED (2026-05-10) |
| T12 | Trinity security review | Trinity | ✅ Complete — ACCEPTED (2026-05-10) |
| T00 | Implementation kickoff package | Tank | ✅ Complete — implementation contract captured in `implementation-plan.md` (2026-05-10) |
| T01 | Protected environment + OIDC workflow contract | Tank | ✅ Complete — protected OIDC contract scaffolded in `.github\workflows\m8-live-oidc-contract.yml` (2026-05-10) |
| T02 | PR validation workflow | Tank / Neo | ✅ Complete — public-safe CI guardrails added in `.github/workflows/ci.yml` + `tools/ci/public_safe_validation.py` (2026-05-10) |
| T03 | Manual deploy-live workflow | Tank | ✅ Complete — protected manual deploy-live scaffold added in `.github\workflows\m8-deploy-live.yml` (2026-05-10) |
| T04 | Start-resume workflow | Tank | ✅ Complete — manual protected start/resume scaffold added in `.github\workflows\m8-start-resume.yml` (2026-05-10) |
| T05 | Nightly shutdown workflow | Tank | ✅ Complete — protected nightly shutdown scaffold added in `.github\workflows\m8-nightly-shutdown.yml` (2026-05-10) |
| T06 | Optional destroy workflow | Tank | ⚪ Not implemented — optional/non-blocking for M8 closeout |
| T07 | Canonical browser smoke + trace workflow | Neo / Mouse | ✅ Complete — protected canonical smoke+trace scaffold added in `.github\workflows\m8-smoke-trace.yml` with static harness and hard-fail trace contract wiring (2026-05-10) |
| T08 | Security and telemetry validation gates | Trinity / Tank / Neo | ✅ Complete — static/local workflow policy and KQL contract gates added in `tools\ci\public_safe_validation.py`, `tools\telemetry\validate_m8_kql_contract.py`, `tools\telemetry\kql\`, and `tests\security\test_m8_validation_gates.py`; T14 remediation coverage re-review accepted by Trinity (2026-05-10). |
| T09 | Cost model and operator documentation | Tank | ✅ Complete — added M8 operator runbook in `docs\deployment\aca\m8-operator-guide.md` and linked ACA deployment docs (2026-05-10) |
| T13 | Morpheus implementation conformance review | Morpheus | ✅ Complete — ACCEPTED (2026-05-10; conformance verified, no blockers) |
| T14 | Trinity implementation security review | Trinity | ✅ Complete — ACCEPTED (2026-05-10 focused re-review) |
| T15 | Final closeout | Tank | ✅ Complete — ACCEPTED (2026-05-10) |

---

## Milestone Gate Checklist

- [x] Spec 008 directory created: `.squad\specs\008-live-azure-e2e-gate\`
- [x] All eight spec artifacts created
- [x] Tank and Trinity inbox guidance consolidated into the spec artifacts
- [x] `state.json` parses successfully
- [x] `roadmap.md` updated for M8 complete/closed status with public-safe validation summary
- [x] `.squad\identity\now.md` updated to mark M8 complete/closed and set next focus
- [x] T10 complete — Tank deployment review re-reviewed and accepted
- [x] T11 complete — Morpheus architecture review accepted
- [x] T12 complete — Trinity security review accepted
- [x] Implementation started only after T10 + T11 + T12
- [x] Live workflows implemented
- [x] No live Azure smoke execution is claimed in this closeout session
- [x] Cost-control workflows documented and validated
- [x] T13 complete — Morpheus implementation conformance review
- [x] T14 complete — Trinity implementation security review
- [x] T15 complete — final closeout

---

## Open Questions

No open T10 review questions. Implementation sequencing remains governed by the task graph in `tasks.md`.

---

## Review Findings

### T13 — Morpheus implementation conformance review

- **Date:** 2026-05-10
- **Verdict:** **ACCEPTED**
- **Recommended revision owner:** None

**Acceptance findings**

1. Spec 008 required-path implementation matches the approved task graph: T03/T04/T05/T07/T08/T09 are present, and required review/closeout flow does not depend on optional T06 destroy/recreate work.
2. Public CI remains validation-only. `.github\workflows\ci.yml` runs tests/Terraform validation/public-safe checks only, while live workflows remain `workflow_dispatch` and/or protected schedule scoped with protected environments and explicit OIDC boundaries.
3. The workflow family is coherent across OIDC contract, deploy-live, start/resume, nightly shutdown, and smoke/trace. Smoke remains opt-in behind `live_azure_tests=true`, shutdown remains non-destructive by default, and lifecycle reporting uses the required resource classifications.
4. Cost-control and operator docs are aligned with the implementation and do not overclaim stoppability: ACA scales to zero, APIM is tier-dependent, telemetry resources remain billable, and destroy/recreate stays optional/manual only.
5. Naming remains architecturally clear: the browser → APIM → BFF → Agent Execution Service → MCP path is explicit throughout, and optional AKS Agent Gateway references remain clearly distinguished from the Agent Execution Service under test.
6. Spec state/progress remain accurate for this milestone stage: implementation scaffolds and static validation are complete, but no live Azure deploy/smoke execution is claimed.

---

### T10 — Tank deployment review

- **Date:** 2026-05-10
- **Verdict:** **ACCEPTED**
- **Recommended revision owner:** None

**Acceptance findings**

1. `design.md` keeps `m8-destroy.yml` optional/non-blocking for M8 closeout unless T06 is explicitly implemented, resolving the original destroy-scope ambiguity.
2. `tasks.md` no longer uses broad `T03–T09` dependency ranges in required closeout/review paths. T13 and T14 now depend only on `T03, T04, T05, T07, T08, T09`, with T06 included only if explicitly implemented.
3. T15 keeps T06 conditional/non-blocking unless destroy/recreate is explicitly brought into scope, so required M8 closeout no longer depends on optional destroy work.
4. The required cost-control path remains intact and implementable: nightly stop/scale-down plus manual start/resume are still the required path, with destroy remaining an optional enhancement.

---

### T12 — Trinity security review

- **Date:** 2026-05-10
- **Verdict:** **ACCEPTED**
- **Recommended revision owner:** None

**Acceptance findings**

1. The original OIDC/shutdown blocker is resolved. `requirements.md`, `design.md`, and `tasks.md` now make purpose-scoped OIDC separation binding, keep deploy/smoke behind protected environments, require shutdown/start-resume separation or equivalently constrained scope, and require RBAC-scope documentation plus static checks for deploy/smoke/shutdown identities.
2. The original telemetry/KQL blocker is resolved. `design.md` now defines an implementation-binding contract across requests, dependencies, traces, and logs, explicitly bans raw tokens, `Authorization` values, `oid`, `sub`, `email`, `upn`, `preferred_username`, and unsafe `tracestate`, and requires smoke/deploy trace verification to hard-fail on leakage.
3. Other T12 conditions remain satisfied at planning level: public CI stays validation-only; APIM → BFF → Agent Execution Service → MCP delegated/OBO boundaries remain explicit; shutdown permissions remain scoped/idempotent/non-destructive by default; and negative/static scans cover workflow policy, artifact hygiene, token-handling, auth-boundary, and telemetry leakage checks.

---

### T14 — Trinity implementation security review

- **Date:** 2026-05-10
- **Verdict:** **ACCEPTED**
- **Recommended revision owner:** None

**Acceptance findings**

1. `m8-live-oidc-contract.yml` no longer prints live Azure account metadata. All three identity-contract jobs now use non-disclosing checks (`az --version > /dev/null` and `az account show --output none`), resolving T14-01 without weakening the OIDC contract.
2. `m8-start-resume.yml` no longer prints the protected readiness endpoint. The readiness step still uses `M8_READINESS_URL` for the probe, but workflow-visible output is reduced to a suppressed-value message, resolving T14-02.
3. Focused searches across the M8 workflow family found no equivalent workflow-visible account-metadata or endpoint-variable leakage patterns remaining. The reviewed workflows keep summaries placeholder-safe and do not echo live `*URL/*URI/*ENDPOINT/*HOST` variables.
4. Static gates and regressions now cover the rejected patterns. `tools\ci\public_safe_validation.py` flags unsafe `az account show` output and endpoint-variable printing to workflow output commands, and `tests\security\test_m8_public_safe_validation.py` includes regressions for both rejected cases plus safe replacements.

**Validation**

- `python tools\ci\public_safe_validation.py`
- `pytest tests\security\test_m8_public_safe_validation.py -q`
- `python -c "import json, pathlib; json.load(open(r'.squad\\specs\\008-live-azure-e2e-gate\\state.json', encoding='utf-8')); print('state.json parse ok')"`
- Targeted repository searches for `az account show`, `$GITHUB_STEP_SUMMARY`, and `*URL/*URI/*ENDPOINT/*HOST` workflow output patterns across `.github\workflows\m8-*.yml`, `tools\ci\public_safe_validation.py`, and `tests\security\test_m8_public_safe_validation.py`

---

## Log

- 2026-05-10 — Spec 008 created as the planning gate for M8 with all eight artifacts and planning-only ADRs.
- 2026-05-10 — Repository consistency updated: `roadmap.md` points M8 to Spec 008 and `.squad\identity\now.md` reflects planning / opt-in posture.
- 2026-05-10 — Consolidated Tank pipeline/cost-control guidance and Trinity security/telemetry guardrails into `research.md`, `requirements.md`, `design.md`, and `tasks.md`; marked the spec ready for review and kept implementation blocked.
- 2026-05-10 — Validation completed for this planning revision: `state.json` parsed successfully and no Tank/Trinity pending placeholder text remained in the reviewed spec artifacts.
- 2026-05-10 — T10 Tank deployment review rejected the spec for revision. Blocking findings: destroy-workflow scope is still ambiguous, and T09 incorrectly depends on optional T06. Recommended revision owner: Tank.
- 2026-05-10 — T10 remediation submitted by Tank. `design.md` now marks `m8-destroy.yml` as optional/non-blocking for M8 closeout unless T06 is explicitly implemented, and `tasks.md` now keeps required docs/closeout on a path that does not depend on optional T06.
- 2026-05-10 — **T10 RE-REVIEW — REJECTED** (Tank, Azure / Terraform / DevOps Reviewer). The destroy-path scope issue is resolved in `design.md`, but `tasks.md` still says T13 and T14 depend on `T03–T09`, which implicitly includes optional T06 and therefore still blocks required closeout on an optional destroy workflow. Recommended revision owner: Neo. Tank remains locked out on this artifact per reviewer protocol.
- 2026-05-10 — **T10 SECOND REMEDIATION SUBMITTED (Neo)**. Updated `tasks.md` to remove broad dependency ranges that implicitly included optional T06 in required closeout paths: T13/T14 now declare explicit required dependencies (`T03, T04, T05, T07, T08, T09`) plus conditional T06 only if implemented, and T15 now keeps T06 as conditional/non-blocking unless explicitly in scope. T10 remains unaccepted pending Tank re-review.
- 2026-05-10 — **T10 RE-REVIEW — ACCEPTED** (Tank, Azure / Terraform / DevOps Reviewer). Focused validation confirmed the remaining blocker is resolved: required M8 review/docs/closeout paths no longer depend on optional T06, destroy remains optional/non-blocking, and the required nightly shutdown plus manual start/resume path remains intact. Validation for this re-review: `state.json` parsed successfully; targeted dependency search found no remaining broad `T03–T09` dependency ranges in required closeout/review paths; destroy optional/non-blocking language remained aligned across `design.md` and `tasks.md`.
- 2026-05-10 — **T11 COMPLETE — ACCEPTED** (Morpheus, Lead / Architect). Reviewed Spec 008 against the M8 architecture scope and found it coherent for implementation planning: (1) opt-in live Azure E2E remains isolated from public CI defaults, (2) the browser → APIM → BFF → Agent Execution Service → MCP path is explicit and consistent with prior milestones, (3) the pipeline-first deploy/start/stop/smoke model fits the repo's existing milestone pattern, (4) nightly shutdown and cost-control requirements are prominent and traceable to Ashley's directive, and (5) Agent Execution Service naming is clearly separated from optional AKS Agent Gateway concerns. No blocking architecture revisions required. Implementation remains blocked pending T10 and T12.
- 2026-05-10 — **T12 REVIEW — REJECTED** (Trinity, Identity & Security Engineer). Public CI remains validation-only, placeholder hygiene is intact, and the delegated APIM → BFF → Agent Execution Service → MCP boundary is preserved at planning level, but two blocking findings keep T12 unaccepted. **T12-01:** `design.md` weakens the mandatory least-privilege OIDC model by making separate deploy/smoke/shutdown principals merely "preferred" instead of binding; the shutdown identity also needs explicit stop/scale-only scope with no routine deploy/smoke reuse. **Recommended revision owner:** Tank. **T12-02:** `requirements.md` promises negative telemetry proof across requests, dependencies, traces, and logs, but `design.md` only defines an illustrative `union requests, dependencies, traces` recipe and a generic `tracestate` presence check; the spec needs an implementation-binding KQL contract for all required log surfaces and safe `tracestate` handling. **Recommended revision owner:** Neo. Focused validation performed during review: manual review of `.squad\decisions\inbox\trinity-m8-security-guardrails.md`, all Spec 008 artifacts, `.github\workflows\ci.yml`, and `infra\terraform\environments\single-tenant-aca\`; no live auth, Azure mutation, or secret material used.
- 2026-05-10 — **T12-01 REMEDIATION SUBMITTED (Tank)**. Updated `requirements.md`, `design.md`, and `tasks.md` to make purpose-scoped OIDC principals and least-privilege RBAC binding (not preferred), require protected-environment/manual approval boundaries for deploy/smoke, require shutdown identity separation/equivalent constraint with stop/start/scale-only scope, and add acceptance/static-check expectations for principal separation plus RBAC scope documentation. T12 remains unaccepted pending Trinity re-review.
- 2026-05-10 — **T12-02 REMEDIATION SUBMITTED (Neo)**. Updated `design.md` and `tasks.md` to replace illustrative telemetry checks with a binding KQL contract that explicitly covers requests/dependencies/traces/logs, adds explicit forbidden token/PII/tracestate checks (`Authorization`, token/JWT patterns, `oid`, `sub`, `email`, `upn`, `preferred_username`), and requires smoke/deploy trace verification to hard-fail on leakage results. Added acceptance criteria for versioned KQL files, runner wiring, and static/local contract tests without performing live Azure queries in this revision. T12 remains unaccepted pending Trinity re-review.
- 2026-05-10 — **T12 COMPLETE — ACCEPTED** (Trinity, Identity & Security Engineer). Focused re-review confirms both original blockers are resolved: purpose-scoped OIDC/shutdown least privilege is now binding, and telemetry verification is now an implementation-binding KQL contract across requests/dependencies/traces/logs with explicit token/PII/`tracestate` bans and hard-fail behavior. Other T12 security conditions remain satisfied in the planning artifacts, including protected-environment/public-CI separation, delegated APIM → BFF → Agent Execution Service → MCP boundaries, scoped/idempotent shutdown expectations, and required negative/static scan coverage. Validation for this re-review: `state.json` parsed successfully; targeted search found no remaining weak `preferred`/illustrative wording in active spec requirements/design for the resolved blockers; `python -m pytest -q tests\security\test_telemetry.py` passed (34 passed). Implementation remains blocked pending T10 re-review.

- 2026-05-10 — **T00 COMPLETE — IMPLEMENTATION KICKOFF PACKAGE FINALIZED** (Tank). Added `.squad\specs\008-live-azure-e2e-gate\implementation-plan.md` as the downstream implementation contract for T01–T05/T07/T08, including finalized workflow names, job boundaries, workflow inputs/outputs, dependency handoff mapping, resource lifecycle matrix (ACA/APIM/Log Analytics/App Insights/ACR + optional AKS/Agent Gateway), protected GitHub Environment/OIDC least-privilege model, and public-safe placeholder env/secrets contract. Validation: `state.json` parse check and manual cross-file consistency review completed with no live Azure mutations.

- 2026-05-10 — **T02 COMPLETE — PR VALIDATION WORKFLOW** (Tank / Neo). Extended `.github\workflows\ci.yml` with Terraform `init -backend=false` + `validate` for `infra\terraform\environments\single-tenant-aca` and replaced inline checks with `tools\ci\public_safe_validation.py` guardrails. Added static/local CI policy coverage test `tests\security\test_m8_public_safe_validation.py` and telemetry contract token checks (offline only, no live Azure queries). Validation: `python tools/ci/public_safe_validation.py`, `python -m pytest -q tests\security\test_telemetry.py tests\security\test_m8_public_safe_validation.py`, and `python -m pytest -q` all passed.

- 2026-05-10 — **T01/T04/T05 COMPLETE — LIVE OPERATIONS SCAFFOLDS ADDED** (Tank). Added protected OIDC contract workflow (`.github\workflows\m8-live-oidc-contract.yml`) plus start/resume and nightly shutdown workflows (`m8-start-resume.yml`, `m8-nightly-shutdown.yml`) with OIDC placeholder secrets (`AZURE_CLIENT_ID_DEPLOY`, `AZURE_CLIENT_ID_SMOKE`, `AZURE_CLIENT_ID_SHUTDOWN`, `AZURE_TENANT_ID`, `AZURE_SUBSCRIPTION_ID`) and protected environments (`lab-live-azure-deploy`, `lab-live-azure-smoke`, `lab-live-azure-ops`). Lifecycle workflows remain idempotent and non-destructive by default, emit required action classifications, and keep destroy behavior out of scheduled paths. Updated operator docs in `docs\deployment\aca\README.md` and `docs\deployment\azure-container-apps.md`. Validation: YAML parse checks, state parse check, static scans for GUID/secrets patterns, and destructive-command scan for scheduled shutdown path.
- 2026-05-10 — **T03 COMPLETE — MANUAL DEPLOY-LIVE WORKFLOW** (Tank). Added protected manual deploy-live scaffold `.github\workflows\m8-deploy-live.yml` with explicit workflow_dispatch inputs, protected environment boundaries, OIDC deploy identity login, IaC/config/image/app rollout stage boundaries using dry-run-safe defaults, and smoke stage opt-in gate via `live_azure_tests=true` in protected smoke scope only. Preserved APIM delegated Authorization boundary and Agent Execution Service OBO naming in workflow summaries/docs. Validation: workflow YAML parse, `python tools/ci/public_safe_validation.py`, and `python -m pytest -q` passed.

- 2026-05-10 — **T07 COMPLETE — CANONICAL BROWSER SMOKE + TRACE WORKFLOW** (Neo / Mouse). Added protected `.github\workflows\m8-smoke-trace.yml` with `workflow_dispatch` + `workflow_call`, explicit `live_azure_tests=true` gate, static/local browser wiring validation (`tools\ci\m8_browser_smoke_harness.py`), and trace contract validation/evaluation (`tools\telemetry\validate_m8_kql_contract.py`, `tools\ci\m8_smoke_trace_contract.py`) that represents hard-fail behavior for leakage findings. Updated `.github\workflows\m8-deploy-live.yml` smoke stage to delegate to canonical workflow, added security tests in `tests\security\test_m8_smoke_trace_contract.py`, and updated ACA deployment docs. Validation: `python tools\ci\public_safe_validation.py`, `python -m pytest -q tests\security\test_telemetry.py tests\security\test_m8_public_safe_validation.py tests\security\test_m8_smoke_trace_contract.py`, and `python -m pytest -q` passed.
- 2026-05-10 — **T08 COMPLETE — SECURITY AND TELEMETRY VALIDATION GATES** (Trinity / Tank / Neo). Expanded `tools\ci\public_safe_validation.py` to enforce protected workflow/OIDC constraints, identity-separation placeholders, lifecycle least-privilege command bans, RBAC scope documentation signals, artifact hygiene, and secret/token-literal scans. Added versioned KQL contract artifacts in `tools\telemetry\kql\` plus offline contract validator `tools\telemetry\validate_m8_kql_contract.py`, wired deploy smoke and CI to run contract validation, and added regression tests in `tests\security\test_m8_validation_gates.py`.
- 2026-05-10 — **T09 COMPLETE — COST MODEL AND OPERATOR DOCUMENTATION** (Tank). Added `docs\deployment\aca\m8-operator-guide.md` covering protected operator flow (deploy, start/resume, smoke/trace, nightly shutdown), cost behavior by resource type (ACA/APIM/Log Analytics/App Insights/ACR/managed identities + optional AKS Agent Gateway), required RBAC/identity boundaries, optional T06 destroy/recreate implications, and safe smoke/KQL leakage invocation without token/PII exposure. Updated `docs\deployment\aca\README.md` and `docs\deployment\azure-container-apps.md` to reference the runbook. Validation completed: `python tools\ci\public_safe_validation.py`, `python tools\telemetry\validate_m8_kql_contract.py`, `python tools\ci\m8_smoke_trace_contract.py validate --workflow-file .github\workflows\m8-smoke-trace.yml --positive-kql tools\telemetry\kql\m8-positive-chain.kql --negative-kql tools\telemetry\kql\m8-negative-leakage.kql`, and `state.json` parse check passed; static scan confirmed no real IDs/secrets/tokens in updated docs.
- 2026-05-10 — **T13 COMPLETE — ACCEPTED** (Morpheus, Lead / Architect). Post-implementation conformance review verified Spec 008 implementation matches the approved architecture and task graph for the required path: public CI remains validation-only, live workflows remain protected/manual or scheduled shutdown only, optional T06 destroy/recreate does not gate required closeout, workflow family boundaries are coherent, cost-control docs avoid overclaiming stoppability, and Agent Execution Service naming remains distinct from optional AKS Agent Gateway references. Focused validation passed: `state.json` parse, M8 workflow YAML parse, `python tools\ci\public_safe_validation.py`, `python tools\ci\m8_browser_smoke_harness.py --mode static`, `python tools\telemetry\validate_m8_kql_contract.py`, and `python tools\ci\m8_smoke_trace_contract.py validate --workflow-file .github\workflows\m8-smoke-trace.yml --positive-kql tools\telemetry\kql\m8-positive-chain.kql --negative-kql tools\telemetry\kql\m8-negative-leakage.kql`. No live Azure actions were run or claimed.
- 2026-05-10 — **T14 REVIEW — REJECTED** (Trinity, Identity & Security Engineer). Purpose-scoped OIDC placeholders, public-safe CI separation, shutdown least-privilege posture, and offline telemetry/KQL validation are implemented and the focused security validation suite passed, but two blockers keep T14 unaccepted. **T14-01:** `.github\workflows\m8-live-oidc-contract.yml` runs `az account show --output table` in all three identity-contract jobs, which will print live tenant/subscription/account context into workflow logs once protected environments are wired. **T14-02:** `.github\workflows\m8-start-resume.yml` writes `${M8_READINESS_URL}` directly into `$GITHUB_STEP_SUMMARY`, exposing real protected-environment endpoint details, and current T08 static gates/tests do not detect either output pattern. **Recommended revision owner:** Tank. Focused validation performed during review: `python -c "import json; json.load(open(r'.squad\\specs\\008-live-azure-e2e-gate\\state.json', encoding='utf-8')); print('state.json OK')"`; `python tools\ci\public_safe_validation.py`; `python tools\telemetry\validate_m8_kql_contract.py`; `python tools\ci\m8_smoke_trace_contract.py validate --workflow-file .github\workflows\m8-smoke-trace.yml --positive-kql tools\telemetry\kql\m8-positive-chain.kql --negative-kql tools\telemetry\kql\m8-negative-leakage.kql`; `python -m pytest -q tests\security\test_m8_public_safe_validation.py tests\security\test_m8_smoke_trace_contract.py tests\security\test_m8_validation_gates.py tests\security\test_telemetry.py` (`43 passed`). T14 remains pending until those workflow-output exposures and their offline guards are remediated.
- 2026-05-10 — **T14 STATIC-GATE REMEDIATION SUBMITTED — PENDING RE-REVIEW** (Neo, Python / Agent Backend Engineer). Extended `tools\ci\public_safe_validation.py` to flag `az account show` metadata logging and live endpoint variable output (`*URL/*URI/*ENDPOINT/*HOST`) to workflow summaries/plain logs. Added fixture-based regressions in `tests\security\test_m8_public_safe_validation.py` so rejected patterns fail while safe replacements pass. This update does **not** mark T14 accepted; Trinity re-review remains required.
- 2026-05-10 — **T14 WORKFLOW-OUTPUT REMEDIATION SUBMITTED — PENDING RE-REVIEW** (Tank, Azure / Terraform / DevOps Engineer). Updated `.github\workflows\m8-live-oidc-contract.yml` to replace `az account show --output table` with non-disclosing checks (`az --version` and `az account show --output none`) across deploy/smoke/lifecycle identity jobs. Updated `.github\workflows\m8-start-resume.yml` readiness summary output to suppress endpoint value disclosure (`Readiness probe passed (endpoint value suppressed).`). Ran focused M8 workflow exposure scan plus YAML/public-safe/state validation. T14 remains **pending Trinity re-review** and is **not accepted** in this revision.
- 2026-05-10 — **T14 RE-REVIEW — ACCEPTED** (Trinity, Identity & Security Engineer). Focused security re-review confirms both blockers are resolved: the OIDC contract workflow no longer prints tenant/subscription/account metadata, and the start/resume workflow no longer emits the protected readiness endpoint in workflow-visible output. Targeted searches found no equivalent account-metadata or endpoint-variable leakage patterns across the reviewed M8 workflows. Offline/public-safe validation now catches the rejected patterns via `tools\ci\public_safe_validation.py` and `tests\security\test_m8_public_safe_validation.py`. Validation for this re-review: `python tools\ci\public_safe_validation.py`; `pytest tests\security\test_m8_public_safe_validation.py -q`; `state.json` parse check; targeted leak-pattern searches in `.github\workflows\m8-*.yml`, `tools\ci\public_safe_validation.py`, and `tests\security\test_m8_public_safe_validation.py`.
- 2026-05-10 — **T15 COMPLETE — FINAL CLOSEOUT** (Tank). Reconciled task statuses for required-path completion (T00/T01/T02/T03/T04/T05/T07/T08/T09 complete; T10/T11/T12/T13/T14 accepted; T15 complete) and recorded T06 as optional/non-blocking and not implemented. Updated `state.json`, `.progress.md`, `.squad\project\roadmap.md`, and `.squad\identity\now.md` to close M8 and keep public-safe posture explicit. Final validation completed: `python -c "import json; json.load(open(r'.squad\\specs\\008-live-azure-e2e-gate\\state.json', encoding='utf-8')); print('state.json parse ok')"`, `python tools\ci\public_safe_validation.py`, `python tools\telemetry\validate_m8_kql_contract.py`, `python -m pytest -q tests\security\test_m8_public_safe_validation.py`, and targeted static scans for GUID/secret/token patterns and live-execution wording. No live Azure deployment, workflow dispatch, or smoke execution was run or claimed in this closeout session.
