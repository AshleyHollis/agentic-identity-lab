# Classic Page Snippets

These snippets are safe placeholders for classic SharePoint pages. They do **not** include tenant IDs, client IDs, or tokens.

## Script editor web part example

Use `classic-page-snippet.html` as the markup inside a Script Editor Web Part.

## Reminder

`userId` is a display hint only. Identity must be validated from access tokens in the BFF.
