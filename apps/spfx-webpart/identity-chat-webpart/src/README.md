# src (Placeholder)

This folder will hold SPFx source code once the web part is scaffolded.

## Notes

- Use SPFx token providers to acquire access tokens.
- Call the BFF with `Authorization: Bearer <token>`.
- Treat `userId` as a display hint only. Identity comes from validated tokens.
