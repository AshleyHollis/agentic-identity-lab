# Azure App Service (Optional)

App Service is an optional target for teams that need a simpler hosting model. Use this only if Container Apps is not viable.

## Notes
- Add App Service resources via the `app-service` Terraform module when approved.
- Keep deployment workflows validation-only until OIDC is configured.
