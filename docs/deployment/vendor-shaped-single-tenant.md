# Vendor-Shaped Single-Tenant Deployment

Vendor-shaped single-tenant deployments model partner-specific identity and routing patterns.

## Terraform
- Environment: `infra/terraform/environments/vendor-shaped-single-tenant`
- Add vendor-specific identity configuration when approved.

## Local dev
Use `docker/docker-compose.vendor-shaped.yml` with the base compose file.
