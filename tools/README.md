# Tools

This directory is reserved for helper scripts that support validation, local setup, token inspection safety checks, and repository maintenance.

## Public Repo Constraints

Tools and scripts in this folder must never include secrets, tenant IDs, subscription IDs, or generated tokens/certificates.
